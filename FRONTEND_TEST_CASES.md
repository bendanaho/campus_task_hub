# 校园互助平台 - 前端与业务功能手工测试用例 (FRONTEND_TEST_CASES.md)

本测试用例说明书专为前端人工交互测试设计。在后端代码仍处于电脑本地运行、未上传至服务器的现阶段，测试人员需在前端将 `USE_MOCK` 开关设置为 `true`（利用 `localStorage` 模拟后端存储与状态机变更）。

通过人工模拟用户操作，利用**有效等价类**验证系统核心业务链条是否通顺，利用**无效等价类**验证前端表单及伪后端对异常输入的精准判断和拦截能力。

---

## 1. 测试前置准备与环境说明
1. **测试环境**：主流现代浏览器（Chrome / Edge / Safari）。
2. **状态初始化**：测试开始前，清除浏览器缓存或执行 `localStorage.clear()`，确保数据环境干净。
3. **Mock配置确认**：检查前端 `js/mock-data.js` 或配置项，确认 `USE_MOCK = true` 已生效。

---

## 2. 模块一：用户账户、认证与钱包（基础模块）

### 2.1 有效等价类测试（正向流程）
| 用例编号 | 测试功能点 | 输入/操作步骤 | 预期输出与系统行为 |
| :--- | :--- | :--- | :--- |
| TC-AUTH-01 | 新用户成功注册 | 1. 打开 `register.html` 注册页面。<br>2. 输入未被占用的用户名 `stud_active`。<br>3. 输入合规手机号 `13812345678`、邮箱 `test@bjut.edu.cn`。<br>4. 输入密码 `123456` 并提交。 | 1. 界面弹出“注册成功”提示。<br>2. 页面自动跳转至 `login.html`。<br>3. 检查 `localStorage` 发现成功持久化该用户记录。 |
| TC-AUTH-02 | 账号成功登录 | 1. 打开 `login.html` 页面。<br>2. 输入刚才注册的用户名 `stud_active` 和密码 `123456`。<br>3. 点击登录。 | 1. 提示“登录成功”。<br>2. 顶栏导航栏右侧正确渲染出当前用户名 `stud_active`。<br>3. 本地生成模拟 JWT Token 并存入 Session。 |
| TC-AUTH-03 | 实名认证通过 | 1. 登录后进入 `profile.html`（个人中心）。<br>2. 点击实名认证，输入真实姓名 `张三`，学号 `20260001`。<br>3. 点击提交认证。 | 1. 界面提示“实名认证成功”。<br>2. 个人中心状态变更为“已实名认证”。<br>3. 账号解锁发布帖子与响应订单的交易权限。 |
| TC-AUTH-04 | 虚拟余额充值 | 1. 在个人中心切换至“我的钱包”或打开 `bill.html`。<br>2. 在充值框输入金额 `100.00` 元。<br>3. 点击“模拟支付”按钮。 | 1. 提示“充值成功”。<br>2. 钱包页面当前可用余额即时从 `0.00` 更新为 `100.00`。<br>3. 账单明细列表新增一条“账户充值 +100.00”的流水记录。 |

### 2.2 无效等价类测试（逆向错误精准判定）
| 用例编号 | 测试功能点 | 输入/操作步骤 | 预期输出与系统行为（错误精准判定） |
| :--- | :--- | :--- | :--- |
| TC-AUTH-05 | 注册用户名重复拦截 | 1. 再次打开注册页面。<br>2. 故意输入已存在的用户名 `stud_active`。<br>3. 输入其他全新合规信息后点击提交。 | 1. 伪后端校验失败，前端精准拦截并弹出红色错误提示：“该用户名已被注册”。<br>2. 阻止表单跳转，数据未重复写入。 |
| TC-AUTH-06 | 密码强度约束拦截 | 1. 注册时输入全新用户名。<br>2. 故意在密码框输入极短或不合规的密码（如 `123`）。<br>3. 点击提交。 | 1. 前端表单即时校验激活，或点击后提示：“密码长度不能少于 6 位”。<br>2. 阻止注册请求的发送。 |
| TC-AUTH-07 | 未实名认证权限限制 | 1. 注册一个全新账号 `user_new`，**不进行实名认证**。<br>2. 直接在导航栏点击“发布互助”或点击他人帖子尝试接单。 | 1. 界面精准拦截，弹出警示框：“请先完成实名认证后再进行相关交易操作”。<br>2. 强制停留在原页面或自动引导重定向至认证页。 |
| TC-AUTH-08 | 充值金额边界校验 | 1. 进入钱包充值页面。<br>2. 故意在输入框输入负数 `-50`，或非数字字符 `abc`，或超大金额。 | 1. 输入框或提交按钮激活防错机制，提示：“请输入合规的正数充值金额”。<br>2. 拦截请求，可用余额不发生任何变动。 |

---

## 3. 模块二：悬赏求助帖交易流转（付款方 Payer 发布模式）

### 3.1 有效等价类测试（正向完整链路）
| 用例编号 | 测试功能点 | 输入/操作步骤 | 预期输出与系统行为 |
| :--- | :--- | :--- | :--- |
| TC-PAY-01 | 成功发布悬赏帖 | 1. 使用已实名且余额为 `100.00` 的账户 A 登录。<br>2. 进入 `publish-task.html`，选择【悬赏求助（Payer）】。<br>3. 输入标题 `代取操场东侧顺丰快递`，报酬 `20.00` 元，选择一个合规的未来截止时间。<br>4. 点击发布。 | 1. 界面提示“发布成功”。<br>2. 跳转回 `task-hall.html`（互助大厅），大厅能立刻看到该求助卡片。<br>3. 此时账户 A 可用余额仍为 `100.00`（尚未生成订单，不扣款）。 |
| TC-PAY-02 | 接单人发起接单申请 | 1. 切换登录已实名的账户 B（此时余额为 `0.00`）。<br>2. 在大厅点击上述“代取快递”帖子进入 `task-detail.html`。<br>3. 点击【我要接单】按钮。 | 1. 界面提示“接单申请已发送，等待发布者接受”。<br>2. 系统后台自动创建订单，状态初始化为 `待接受（pending）`。<br>3. 自动为 A 和 B 建立一对一即时会话。 |
| TC-PAY-03 | 发布者接受接单与资金冻结 | 1. 切换回发布者账户 A 登录，进入 `order-center.html`（我的订单）。<br>2. 找到该笔待接受订单，点击【接受接单】。 | 1. 订单状态成功扭转为 `进行中（in_progress）`。<br>2. **钱包发生核心变动**：账户 A 的可用余额即时扣除 `20.00` 变为 `80.00`，该 `20.00` 被记为**冻结资金**。<br>3. A 与 B 的聊天详情页自动插入居中系统消息。 |
| TC-PAY-04 | 双方确认完成与金钱结算 | 1. 模拟线下完成，账户 A（或账户 B）在订单页点击【确认完成】。<br>2. 另一方同样进入页面点击【确认完成】。 | 1. 订单状态成功扭转为 `已完成（completed）`。<br>2. **钱包完成最终结算**：账户 B 的可用余额即时从 `0.00` 变为 `20.00`（冻结款成功解冻并解给收款方）。<br>3. 双方账单分别记录一笔扣款和收款流水。 |
| TC-PAY-05 | 交易互评机制 | 1. 订单完成后，账户 A 点击【评价】按钮。<br>2. 选择 `5星`，填写评语 `速度很快，好评！`，提交。 | 1. 提示“评价提交成功”。<br>2. 切换至账户 B 的个人中心，其信用分发生基于贝叶斯平滑的即时重算，评价列表中可见 A 的评语。 |

### 3.2 无效等价类测试（逆向状态与边界阻断）
| 用例编号 | 测试功能点 | 输入/操作步骤 | 预期输出与系统行为（错误精准判定） |
| :--- | :--- | :--- | :--- |
| TC-PAY-06 | 余额不足拦截发布 | 1. 切换至一个已实名但可用余额仅有 `5.00` 元的账户 C。<br>2. 尝试发布一个报酬为 `15.00` 元的【悬赏求助】帖子。 | 1. 点击发布时系统触发强控制拦截，弹出错误提示：“您的可用余额不足，无法发布该悬赏（当前可用：5.00，所需：15.00）”。<br>2. 拒绝创建帖子。 |
| TC-PAY-07 | 限制对自己发布的帖子接单 | 1. 账户 A 登录状态下，通过大厅或直接访问链接进入自己发布的“代取快递”详情页。 | 1. 页面检测到发布者 ID 与当前登录者 ID 相同。<br>2. 详情页的【我要接单】按钮自动置灰不可点击，或者点击后提示：“无法对自己发布的帖子进行接单”。 |

---

## 4. 模块三：提供服务帖交易流转（收款方 Earner 发布模式）与争议仲裁流程

### 4.1 有效等价类测试（正向服务交易与申诉闭环）
| 用例编号 | 测试功能点 | 输入/操作步骤 | 预期输出与系统行为 |
| :--- | :--- | :--- | :--- |
| TC-EAR-01 | 发布技能服务帖 | 1. 账户 B（大能手）登录，发布互助选择【提供服务（Earner）】。<br>2. 输入标题 `Java期末考试一对一辅导`，价格定为 `50.00` 元。<br>3. 点击发布。 | 1. 提示“服务上架成功”。<br>2. 大厅对应的“服务大厅”类目下成功展示该卡片。 |
| TC-EAR-02 | 买方下单与资金即时冻结 | 1. 账户 A（余额 `100.00`）登录，在大厅找到账户 B 的服务，进入详情页。<br>2. 点击【立即下单购买】。 | 1. 针对提供服务类，下单即代表同意交易，订单直接生成并进入 `进行中（in_progress）`。<br>2. **买方资金立即锁定**：账户 A 的可用余额即时扣除 `50.00` 变为 `50.00`，这 `50.00` 进入平台的中间冻结池。 |
| TC-EAR-03 | 交易产生分歧发起申诉 | 1. 模拟线下服务产生纠纷（如 B 未提供足额辅导）。<br>2. 买方 A 进入订单中心，对这笔进行中订单点击【申请仲裁/发起申诉】，输入理由“对方迟到且不认真”。 | 1. 订单状态立刻锁定并扭转为 `争议中（disputed）`。<br>2. 此时前台该订单的“确认完成”、“取消”等常规按钮自动隐藏或失效，防止任意一方强行清算。 |
| TC-EAR-04 | 管理员介入处理仲裁结案 | 1. 切换至系统管理员账号（`admin`，`role=1`）登录，进入管理后台 `admin.html`。<br>2. 切换至“待处理申诉”标签页，找到这笔争议订单。<br>3. 点击【执行仲裁】，裁决输入：退回买方 `30.00` 元，结算卖方 `20.00` 元，提交。 | 1. 后台提示“仲裁处理完毕”。<br>2. 订单状态扭转为 `已结案（closed）`。<br>3. **冻结资金精准按裁决划分**：账户 A 钱包退回 `30.00`（余额恢复至 `80.00`）；账户 B 钱包收到 `20.00`（余额变更为 `20.00`）。 |

---

## 5. 模块四：组队互助（None 无金额模式）与过期/举报管理

### 5.1 有效等价类测试
| 用例编号 | 测试功能点 | 输入/操作步骤 | 预期输出与系统行为 |
| :--- | :--- | :--- | :--- |
| TC-NONE-01 | 组队帖完整流转 | 1. 账户 A 发布【组队互助（None）】，标题 `寻找考研数学自习搭子`，报酬设为 `0` 或空。<br>2. 账户 B 登录并点击【申请加入】。<br>3. 双方在订单中心推进流程至完成。 | 1. 组队帖顺利上架并流转。<br>2. 核心校验：在整个生命周期中，账户 A 和账户 B 的钱包可用余额、冻结金额均**始终保持不变（完全不涉及虚拟资金交易）**。 |
| TC-NONE-02 | 违规帖子举报处理 | 1. 账户 B 在浏览大厅时，发现某帖子内容包含垃圾广告。<br>2. 点击帖子卡片下的【举报】按钮，选择理由“违规广告”，提交。 | 1. 前台提示“举报成功，已提交管理员审核”。<br>2. 登录管理员后台，在“举报处理”列表中可以即时查阅到该条举报记录。 |
| TC-NONE-03 | 管理员强制下架 | 1. 管理员在后台针对该被举报的帖子点击【下架帖子】并输入原因。 | 1. 该违规帖子在前端 `task-hall.html`（互助大厅）中被立刻隐藏，普通用户无法再检索到该帖。 |

### 5.2 无效等价类测试（过期与状态反跳约束）
| 用例编号 | 测试功能点 | 输入/操作步骤 | 预期输出与系统行为（错误精准判定） |
| :--- | :--- | :--- | :--- |
| TC-NONE-04 | 截止时间过去时拦截 | 1. 在发布帖子时，故意将截止时间设置为**过去的时间**（例如比当前时间早一小时）。 | 1. 前端时间组件给出警告，或点击发布时提示：“截止时间不能早于当前系统时间”。<br>2. 拦截提交，防止过期垃圾帖子上架。 |
| TC-NONE-05 | 自动下架与不可交互边界 | 1. 修改本地 mock 数据时间，使某一上架帖子的截止时间小于当前模拟系统时间。<br>2. 刷新互助大厅或直接尝试访问其详情页。 | 1. 互助大厅的过滤器自动剔除该帖子，不再展示。<br>2. 若通过直链强行进入详情页，【我要接单/下单】按钮应该置灰，并提示：“该互助需求已过期下架”。 |

---

## 6. 模块五：聊天室交互与动态系统消息

### 6.1 有效等价类测试
| 用例编号 | 测试功能点 | 输入/操作步骤 | 预期输出与系统行为 |
| :--- | :--- | :--- | :--- |
| TC-CHAT-01 | 即时文本消息传输 | 1. 账户 A 进入与账户 B 的订单聊天室 `chat-detail.html`。<br>2. 输入 `请问你在哪个宿舍楼？`，点击发送。 | 1. 账户 A 的聊天气泡即时渲染在右侧。<br>2. 切换到 B 账号，消息中心 `message-center.html` 会话列表出现未读红点提示，进入聊天后消息完整展现。 |
| TC-CHAT-02 | 状态变更触发居中系统消息 | 1. 账户 A 触发了该订单的【接受接单】或【申请仲裁】操作。<br>2. 随后 A 和 B 共同打开对应的聊天详情页。 | 1. 聊天时间轴中间自动插入一条置灰、非气泡的系统全局消息（如：`—— 系统提示：订单状态已变更为进行中，资金已冻结 ——`）。 |

---

## 7. 异常边界与数据回滚专项（无效等价类强校验）

| 用例编号 | 测试功能点 | 恶意/异常边界操作步骤 | 预期阻断与系统自我防御行为 |
| :--- | :--- | :--- | :--- |
| TC-SYS-01 | 绕过流程状态越级跳转 | 1. 某个订单当前处于刚创建的 `待接受（pending）` 状态。<br>2. 恶意绕过页面的“接受”按钮，直接通过 mock 伪造触发【确认完成】按钮对应的底层函数。 | 1. 状态机校验机制激活，拒绝非邻接状态的跳变，抛出错误提示：“当前订单状态不允许执行此操作”。<br>2. 订单强制维持在 `待接受`，资金不释放。 |
| TC-SYS-02 | 结算后超额发起申诉拦截 | 1. 某笔订单已经双方确认，状态已变为 `已完成（completed）` 且虚拟报酬已结算完成。<br>2. 用户尝试通过控制台强行调用发起争议的函数。 | 1. 系统拒绝处理，返回提示：“已完成结算的订单无法发起申诉”。<br>2. 确保已完结的账目数据不可被前端交互逆向篡改。 |