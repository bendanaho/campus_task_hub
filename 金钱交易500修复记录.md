# 金额交易 500 错误修复记录

> 日期：2026-07-13
> 环境：服务器 182.92.133.163，prod profile + 宝塔 MySQL 5.7
> 严重程度：高（所有涉及余额变动的交易全部不可用）

---

## 一、问题现象

服务器上所有涉及金钱（改余额）的请求都返回 500 内部错误：

- 接受订单（冻结付款方余额）
- 确认完成（结算给收款方）
- 账户充值
- 聊天收款 / 转账
- 管理员仲裁退款 / 结算

而创建订单（不扣款）、浏览帖子、查询余额等不涉及余额变动的操作正常。

---

## 二、根因

### 一句话

`users` / `tasks` / `orders` 三张表的 `version` 字段在服务器上全是 **NULL**，而实体加了 `@Version` 乐观锁，导致每次更新已有记录都匹配 0 行，Hibernate 抛 `StaleObjectStateException` → 500。

### 证据链

1. **实体带 `@Version`**：`User`、`Order`、`Task` 三个实体都有
   ```java
   @Version
   private Integer version;
   ```
   （`User.java:87`、`Order.java:109`、`Task.java:101`）

2. **schema 允许 NULL**：`schema.sql` 中三张表的 version 列是
   ```sql
   version INT DEFAULT NULL
   ```

3. **种子数据漏插 version**：`test-data.sql` / `add-test-data.sql` 的 `INSERT` 语句列清单里**没有 version 列**，直接 SQL 插入绕过了 Hibernate。服务器数据由这些脚本灌入，version 列取默认值 NULL。

4. **本地 MySQL 实锤**：
   ```
   UPDATE users SET balance=balance+50 WHERE id=1 AND version=NULL;  → affected 0 行
   UPDATE users SET balance=balance+50 WHERE id=1 AND version=0;     → affected 1 行
   ```

### Hibernate `@Version` 的工作机制

`save()` 一个已存在的实体时，Hibernate 生成带乐观锁条件的 UPDATE：

```sql
UPDATE users SET balance=?, version=version+1 WHERE id=? AND version=?
```

`WHERE ... AND version=?` 用"读出来时的版本号"做条件，只有这期间没人改过这条记录（version 没变）才能更新到，以此实现乐观并发控制。

### version=NULL 为什么会 500

服务器数据 version 全是 NULL，Hibernate 读出实体后 version 字段为 null，生成：

```sql
UPDATE users SET balance=?, version=1 WHERE id=? AND version=NULL
```

SQL 里 `version=NULL` **永远为假**（NULL 与任何值比较结果都是 NULL，不是 true），所以这条 UPDATE **影响 0 行**。

Hibernate 发现 update 影响行数为 0，按乐观锁语义认定"这条记录已被别的事务改过"，抛 `StaleObjectStateException`（`OptimisticLockException` 的子类）。`@Transactional` 方法回滚，异常上抛，全局异常处理返回 500。

---

## 三、为什么本地开发没事，服务器炸

| | 本地 dev（H2） | 服务器 prod（MySQL） |
|---|---|---|
| 种子数据来源 | `DataInitializer` 用 Hibernate `save()` 插入 | `test-data.sql` 直接 `INSERT`（绕过 Hibernate） |
| version 填充 | Hibernate insert 时**自动填 0** | INSERT 漏了 version 列 → 取 schema 默认值 NULL |
| 更新已有实体 | `WHERE version=0` 匹配 1 行，正常 | `WHERE version=NULL` 匹配 0 行，抛异常 |
| 结果 | 金钱交易正常 | 金钱交易全 500 |

关键：`DataInitializer` 带 `@Profile("!prod")`，prod 下不运行，所以 prod 必须靠 SQL 脚本灌数据，而脚本漏了 version。这类 bug 在本地 H2（Hibernate 灌数据）永远复现不了，只有"直接 SQL 灌数据 + 改数据"的真实部署才暴露。

---

## 四、为什么只有金钱交易炸

- **炸**：接受订单 / 确认完成 / 充值 / 聊天付款转账 / 仲裁 —— 都要 `save(已存在的 user)` 改余额，触发 user 的乐观锁 update → version NULL → 500。
- **不炸**：`createOrder` 只 `insert` 一条新订单（新对象，Hibernate insert 自动填 version），不 update 已有 user；浏览 / 查询是 SELECT，不改数据。

---

## 五、修复操作

### 1. 服务器立即修复（已完成，不用重启）

```sql
USE campus_task_hub;
UPDATE users  SET version = 0 WHERE version IS NULL;
UPDATE tasks  SET version = 0 WHERE version IS NULL;
UPDATE orders SET version = 0 WHERE version IS NULL;
```

验证（三条 null_cnt 全为 0 即修复）：

```sql
SELECT 'users' t, COUNT(*) null_cnt FROM users  WHERE version IS NULL
UNION ALL SELECT 'tasks',  COUNT(*) FROM tasks  WHERE version IS NULL
UNION ALL SELECT 'orders', COUNT(*) FROM orders WHERE version IS NULL;
```

实测结果：

```
+--------+----------+
| t      | null_cnt |
+--------+----------+
| users  |        0 |
| tasks  |        0 |
| orders |        0 |
+--------+----------+
```

UPDATE 把 NULL 填成 0 后，乐观锁 `WHERE version=0` 能正常匹配，金钱交易立即恢复。无需重启后端。

### 2. 治本：SQL 脚本改动（本地已改，待提交）

| 文件 | 改动 |
|---|---|
| `db/schema.sql` | 三张表 `version` 改 `INT NOT NULL DEFAULT 0`（新部署即使漏插也填 0，从根上杜绝 NULL） |
| `db/test-data.sql` | users / tasks / orders 三处 INSERT 显式补 `version=0` |
| `db/add-test-data.sql` | tasks INSERT 显式补 `version=0` |

本地 MySQL 已导入验证：语法正确、列数匹配、version 全 0。

> 注意：已部署的服务器表结构不会因改 `schema.sql` 而自动变更（MySQL 不会重建表），所以现有库必须靠上面的 UPDATE 修数据；`schema.sql` 的 `NOT NULL DEFAULT 0` 只对**全新部署**有防御作用。

---

## 六、经验与预防

1. **`@Version` 字段不要留 NULL**：任何直接 SQL 插入的种子 / 迁移数据，必须给 version 赋非 NULL 值（0 即可），或保证 schema 有 `DEFAULT 0`。
2. **schema 的 version 列应 `NOT NULL DEFAULT 0`**：从根上杜绝 NULL，即使 INSERT 漏填也安全。
3. **`ddl-auto=validate` 的盲区**：prod 只校验"列是否存在"，不校验 NULL 约束 / 默认值，所以 schema 写 `DEFAULT NULL` 不会启动报错，只在运行时改数据时踩雷。
4. **本地测试覆盖不到的场景**：本地 H2 用 Hibernate 灌数据，version 自动填值，永远复现不了"直接 SQL 数据 version=NULL"的问题。涉及 `@Version` 的实体，种子数据脚本必须与实体定义对齐，建议在 CI 里用"SQL 脚本建库 + 灌数据"的方式跑一遍集成测试，弥补 H2 的盲区。
