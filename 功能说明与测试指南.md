# 校园互助平台 · 功能说明与测试指南

> 面向：负责测试的组员。本文档梳理前后端全部功能（含新扩展），给出启动方式、测试账号、功能清单、核心业务流程和测试路径。
>
> 更新时间：2026-07-11。已接入 MySQL、补全管理员/举报/争议仲裁/信用分/WebSocket 通知。

---

## 一、项目概述

校园实名互助平台：学生发布悬赏/服务/组队帖，通过聊天沟通、下单、确认完成、互相评价，管理员仲裁争议。

| 端 | 技术栈 | 位置 |
|----|--------|------|
| 前端 | 原生 HTML/CSS/JS（非 Vue/React），无构建工具 | `campus_task_hub/front-end/` |
| 后端 | Spring Boot 4.0.6 + Java 17 + JPA + Spring Security + WebSocket | `backend/Keshe_backend/` |
| 数据库 | dev：H2 内存库 / prod：MySQL 8 | |

**两种运行模式：**
- `USE_MOCK = true`（前端纯演示）：数据走浏览器 localStorage，无需后端。
- `USE_MOCK = false`（前后端联调，默认）：前端请求后端 `http://localhost:8080/api`。

---

## 二、快速启动

### 最快：Windows 一键脚本

```
1. 双击 backend/reset-db.bat      （输入 MySQL 密码，建库 + 导入演示数据）
2. 双击 backend/start-backend.bat  （启动后端，等待 "Started KesheBackendApplication"）
3. 浏览器打开 campus_task_hub/front-end/login.html
```

### 命令行

```bash
# 后端（prod/MySQL）
cd backend/Keshe_backend
./mvnw spring-boot:run -Dspring-boot.run.profiles=prod

# 或后端 dev（H2 内存库，自带种子，零依赖）
./mvnw spring-boot:run

# 前端：无需启动服务，直接浏览器打开 login.html
#   （或用 VS Code Live Server 打开 index.html）
```

> 前端 `js/api.js` 第 1-2 行：`USE_MOCK = false`、`API_BASE = 'http://localhost:8080/api'`。改后端地址改这里。

---

## 三、测试账号

### prod 环境（MySQL + test-data.sql，密码均为 `123456`）

| 账号 | 密码 | 角色 | 说明 |
|------|------|------|------|
| `xiaoming` | 123456 | 普通用户 | 余额 100，已认证 |
| `xiaohong` | 123456 | 普通用户 | 余额 50.50，已认证 |
| `xiaowang` | 123456 | 普通用户 | 未认证（测实名门禁） |
| `admin` | 123456 | 管理员 | 仲裁/下架/删除 |

### dev 环境（H2 + DataInitializer，密码均为 `1`）

| 手机号 | 用户名 | 推荐测试场景 |
|--------|--------|--------------|
| 13800138001 | 张三 | 消息最丰富：5 个聊天，涵盖所有角色 |
| 13800138002 | 李四 | 3 个聊天，含已完成/进行中订单 |
| 13800138003 | 王同学 | PPT 服务申请者 |
| 13800138004 | 陈同学 | 0 聊天，未参与交易 |
| 13800138005 | 赵同学 | 高数辅导服务提供者 |
| 13800138007 | 孙同学 | 快递服务提供者 |
| 13800138008 | 周同学 | 早餐代买服务提供者 |
| 13800138009 | admin | **管理员** |

> 测试前清浏览器 localStorage：F12 -> Application -> Local Storage -> 删 `campus_mock_db` / `campus_token`。

---

## 四、前端功能总览（14 个页面）

### 普通用户页面

| 页面 | 功能 |
|------|------|
| `login.html` | 登录（用户名/手机号/邮箱） |
| `register.html` | 注册（用户名/手机号/邮箱/密码校验） |
| `auth.html` | 实名认证（姓名+学号+学院+班级，发帖/接单/付款前置） |
| `index.html` | 首页：简介 + 推荐任务（4 条最新帖） |
| `task-hall.html` | **大厅**：按类型(悬赏/服务/互助)/分类(8类)/关键词/排序筛选，分页，接单/举报 |
| `task-detail.html` | 帖子详情：撤回自己的帖 / 接单 / 看状态 |
| `publish-task.html` | **发帖**：三种类型字段动态切换，图片上传（最多3张） |
| `message-center.html` | 消息中心：会话列表 + 状态/角色/关键词筛选 + 未读 |
| `chat-detail.html` | **聊天**：文本/付款卡片/撤回/订单操作按钮（最复杂） |
| `order-center.html` | 我的订单：进行中概览 + 付款/收款视角筛选 |
| `review.html` | 评价：5 星 + 内容，影响信用分 |
| `profile.html` | 个人中心：资料/充值/认证/信用分 |
| `bill.html` | 账单：按月分组，收支汇总 |

### 管理员页面

| 页面 | 功能 |
|------|------|
| `admin.html` | **管理后台**（4 Tab）：待处理申诉 / 举报处理 / 全部订单 / 帖子管理 |

管理员登录强制跳 `admin.html`，导航栏隐藏"发布/消息/订单"，新增"管理后台"。管理员不能发帖/接单/付款/举报（纯管理角色）。

---

## 五、后端功能总览（8 模块 / 40+ 接口）

| 模块 | 核心接口 | 功能 |
|------|----------|------|
| 认证 | `/api/login` `/api/register` | 登录/注册/登出，JWT |
| 用户 | `/api/user/balance` `/api/user/recharge` `/api/auth` | 余额/充值/账单/实名 |
| 帖子 | `/api/posts` `/api/posts/{id}/close` `/api/posts/{id}/report` | 大厅/详情/发布/撤回/举报 |
| 订单 | `/api/orders` `/api/orders/{id}/accept` `/dispute` `/confirm` | 创建/接受/取消/申诉/确认 |
| 聊天 | `/api/conversations` `/api/messages/{id}/pay` | 消息/付款卡片/撤回/未读 |
| 评价 | `/api/reviews` | 提交/查看 + 信用分计算 |
| 管理员 | `/api/admin/disputes` `/api/admin/orders/{id}/resolve` `/api/admin/reports` | 仲裁/帖子管理/举报 |
| 举报 | `/api/posts/{id}/report` | 用户举报 + 管理员处理 |

> 完整接口字段/响应详见 `backend/CODE_DOCUMENTATION.md`。

---

## 六、★ 新功能专项（测试重点）

### 1. 争议仲裁（订单 disputed -> closed）
- **触发**：in_progress 订单任一方点"申诉" -> 资金冻结。
- **仲裁**：管理员在 `admin.html` 待处理申诉 Tab，可"查看聊天记录"（只读取证），裁决三选一：
  - **全额退款**给付款方
  - **全额结算**给收款方
  - **部分结算**（输入给收款方金额，余款退付款方）
- **结果**：订单 closed，按裁决转账+记账单，发系统消息给双方。**closed 订单不可评价**。
- **测试路径**：xiaoming 发悬赏帖 -> xiaohong 接单 -> 任一方申诉 -> admin 登录仲裁。

### 2. 信用分贝叶斯计算
- **公式**：`score = (5.0 × 2 + Σ评分) / (2 + 评价数)`，先验 C=2 条 5 星。
- **效果**：新用户 5.0；收到评价后自动重算，限 [0,5] 保留 1 位小数。
- **触发**：每次真实评价 / 默认好评后重算。
- **测试**：评价后去个人中心看信用分变化；大厅卡片信用分颜色（≥4.5绿/≥3.0黄/否则红）。

### 3. WebSocket 实时通知
- **连接**：`ws://localhost:8080/ws/notification/{userId}`，登录后自动连。
- **4 类消息**：
  - `NEW_TASK`：大厅插入新帖卡片（高亮闪烁）
  - `TASK_TAKEN`：悬赏帖被接，大厅移除卡片
  - `PERSONAL_NOTICE`：订单状态变更，右上角横幅
  - `CHAT_UPDATE`：聊天页/消息中心局部刷新
- **测试**：开两个浏览器分别登录，一方操作看另一方是否实时刷新。

### 4. 系统通知会话
- 每用户专属会话 `sys-notify-<userId>`，用于帖子被下架/删除等平台通知。
- `senderId=0` 计入未读；与订单系统消息（`senderId=null` 不计未读）区分。
- **测试**：admin 下架 xiaoming 的帖 -> xiaoming 消息中心收到系统通知。

### 5. 自动确认 + 默认好评（定时清扫）
- 单方确认后设 `autoConfirmAt = now + 2天`。
- SweepService 每 5 分钟扫描：超时 in_progress 自动完成；超期未评价自动补 5 星默认好评。
- **测试**：dev 种子 o6（高数辅导）付款方已确认，可触发自动确认。

### 6. 聊天付款卡片
- **转账(transfer)**：发起即扣款到账，status=paid。
- **收款(request)**：生成待支付卡片，付款方点"支付"才扣款，发起方可"取消"。
- 余额不足时支付按钮禁用标"余额不足"。
- **测试**：聊天页点"收款"/"转账"按钮。

### 7. 图片上传 + 缩略图
- 发帖最多 3 张，前端 base64 预览；后端生成缩略图（400px 宽 JPEG），存 `[{full,thumb}]` JSON。
- prod 用 LONGTEXT（base64 原图超 64KB）。
- **测试**：发帖传图 -> 大厅看缩略图 -> 详情看原图 -> 点图弹大图。

### 8. 管理员只读取证
- 仲裁时管理员可查任意会话聊天记录（`chat-detail.html?admin=1`），跳过参与者校验。
- 隐藏输入区，不标已读，不创建会话。

---

## 七、核心业务流程

### 流程 1：订单全生命周期（最常测）

```
1. A 发帖（payer 悬赏 / earner 服务 / none 互助）
2. B 点"接单" -> 创建会话 + 订单 [pending]
   系统："B 申请接单，等待接受" + WebSocket 通知 A
3. A（发布者）接受 [pending -> in_progress]
   金额>0：冻结付款方 B 余额
   悬赏帖：帖子 closed，广播 TASK_TAKEN
4. 任一方确认（单方）-> 设 autoConfirmAt = now+2天
5. 另一方确认（双方）[in_progress -> completed]
   结算给收款方，设 reviewDeadline = now+2天
6. 评价期内任一方评价 -> 重算被评价者信用分
7. 过期未评价 -> SweepService 补 5 星默认好评
```

**分支：** 步骤 3 前 B 可取消（pending）；步骤 4-5 间任一方可申诉（-> disputed -> admin 仲裁 -> closed）。

### 流程 2：争议仲裁

```
in_progress --dispute(任一方)--> disputed（资金冻结）
   --admin 查聊天取证--> admin 裁决(refund/settle/partial) --> closed
   按裁决转账 + 记账单 + 系统消息 + WebSocket 通知
   closed 不可评价
```

### 流程 3：信用分计算

```
新用户 5.0 -> 收到评价 -> recalcCreditScore:
  score = (5.0*2 + Σ评分) / (2 + 评价数)
  限 [0,5]，1 位小数
默认好评（5星）也触发重算
```

### 流程 4：帖子三类型资金流向

| 类型 | 发布者角色 | 接单者角色 | 金额 |
|------|-----------|-----------|------|
| payer（悬赏） | 付款方 | 收款方 | reward_value |
| earner（服务） | 收款方 | 付款方 | reward_value |
| none（互助） | earner | payer | 0 |

---

## 八、测试路径建议（按场景）

### 场景 A：基础发帖接单全流程（必测）
1. xiaoming 登录 -> 发悬赏帖（带图）
2. 退出，xiaohong 登录 -> 大厅看到帖 -> 点"接单"
3. xiaoming 登录 -> 消息中心 -> 进聊天 -> 点"接受订单"
4. 双方各点"确认完成"
5. 任一方去评价（5 星）-> 看信用分变化

### 场景 B：争议仲裁（新功能必测）
1. xiaoming 发悬赏帖 -> xiaohong 接单 -> xiaoming 接受
2. 任一方点"申诉"，填理由
3. admin 登录 -> 管理后台 -> 待处理申诉 -> "查看聊天记录"取证
4. 选"部分结算" -> 输入金额 -> 填处理说明 -> 裁决
5. 回 xiaoming/xiaohong 看订单 closed + 账单 + 系统消息

### 场景 C：聊天付款卡片
1. xiaoming 和 xiaohong 进聊天
2. 点"收款" -> 输入金额 -> xiaohong 看"待支付" -> 点"支付"
3. 点"转账" -> 输入金额 -> 确认 -> 即时到账
4. 右键消息测撤回（2 分钟内）

### 场景 D：管理员帖子管理 + 举报
1. xiaoming 发帖 -> xiaohong 举报（填理由）
2. admin 登录 -> 举报处理 Tab -> 看举报 -> "下架"或"删除"
3. xiaoming 消息中心收到系统通知

### 场景 E：实名门禁 + 信用分展示
1. xiaowang（未认证）登录 -> 发帖/接单 -> 提示"请先实名认证"
2. 去实名认证 -> 再操作
3. 大厅看信用分颜色（绿/黄/红）

### 场景 F：WebSocket 实时（开两个浏览器）
1. 浏览器1 xiaoming 发帖 -> 浏览器2 xiaohong 大厅自动出卡片
2. xiaohong 接单 -> xiaoming 右上角横幅 + 聊天刷新

---

## 九、前后端联调配置

### 前端连后端（2 处）

**`front-end/js/api.js`（HTTP）：**
```js
const USE_MOCK = false;                          // true=纯前端演示
const API_BASE = 'http://localhost:8080/api';    // 后端地址
```

**`front-end/js/main.js`（WebSocket，约第 2233 行）：**
```js
ws = new WebSocket('ws://localhost:8080/ws/notification/' + currentUser.id);
```

### 后端连数据库

`Keshe_backend/src/main/resources/application-prod.yml`（gitignore，见 `.example`）：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_task_hub?useUnicode=true&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8
    username: root
    password: <你的MySQL密码>
  jpa:
    hibernate:
      ddl-auto: validate   # 只校验不建表
```

### localStorage key（清缓存用）
- `campus_token` / `campus_current_user`（登录态）
- `campus_mock_db`（mock 模式数据）

---

## 十、常见问题

| 现象 | 排查 |
|------|------|
| 前端打开空白/报错 | 后端没起，或 `USE_MOCK=false` 但 8080 没服务 |
| 登录失败 "账号或密码错误" | dev 密码 `1`，prod 演示密码 `123456`，确认环境 |
| 发帖提示"请先实名认证" | 未认证用户不能发帖/接单/付款，去 auth.html 认证 |
| prod 启动报 `Schema-validation` | 表结构与实体不一致，重新执行 `db/schema.sql` |
| prod 连不上 MySQL `Public Key Retrieval` | JDBC URL 加 `allowPublicKeyRetrieval=true` |
| 聊天不实时 | WebSocket 没连上，检查 main.js 的 ws 地址 + 后端 8080 |
| 数据脏了想重置 | 双击 `reset-db.bat`，或 dev 重启后端（H2 清空重建） |
| 评价页图片上传无效 | 已知问题：review.html 缺图片元素，JS no-op，不影响评价功能 |

---

## 十一、相关文档索引

| 文档 | 内容 |
|------|------|
| 本文档 | 功能说明 + 测试指南（前后端综合） |
| `backend/CODE_DOCUMENTATION.md` | 后端完整 API/状态机/设计决策 |
| `backend/Readme.md` | 后端概览 + 启动 |
| `backend/TODO.md` | 开发任务清单（含已完成/待办） |
| `backend/Keshe_backend/DATABASE.md` | 数据库设计完整说明（8 表） |
| `backend/db/QUICKSTART.md` | MySQL 快速启动 + 测试用例 + .bat |
| 《部署速查单》.md | 服务器部署（Nginx + systemd） |
| `campus_task_hub/front-end/预置测试账号.txt` | dev 账号详细验证场景 |
