# 校园互助平台 · 微信支付接入说明

> 面向:前端组员(H5 + 小程序)
> 编写:后端
> 日期:2026-07-15
> 配套文档:**《微信支付接口协议.md》**(详细接口字段、交易信息传输协议、时序图)

---

## 术语

- **H5**:网页前端(HTML/CSS/JS,浏览器访问),即当前 `front-end` 那套(`register.html`、`task-hall.html` 等)
- **小程序**:微信小程序前端(wxml/wxss,在微信内运行)
- **商户号 mchid**:微信支付收付款主体(**已有**)
- **AppID**:小程序 / 公众号的身份证,和商户号是**不同的东西**(见第六节)

---

## 一、先澄清:转账卡片 ≠ 真实微信支付

组员之前给的《转账卡片-后端改动需求.docx》里的"转账",是**平台内部余额转账**(A 余额减、B 余额加,改成"托管待收款"模式),**不是真实微信支付**:

- 不涉及商户号、回调、证书、真实资金
- 钱始终在平台 `User.balance` 内部流转
- 难度不大,改造 `ChatService` 即可,和微信支付互不依赖

**真实微信支付**(本文档主题)是钱真实进出微信商户号:充值(钱进)+ 提现(钱出)。这才是有难度的部分。

两者是独立的两条线,可并行,互不阻塞。**转账卡片不需要走真实交易**,可以放心单独做。

---

## 二、后端现状(钱现在怎么走)

| 现状 | 说明 |
|---|---|
| 纯平台余额制 | `User.balance` 内部流转,无真实资金 |
| `recharge()` | 凭空 `balance += amount`,**没有真实付款**(钱没进平台) |
| 订单交易 | 余额内冻结/结算(acceptOrder 冻结、confirmOrder 结算) |
| 聊天转账 | 余额互转 |
| 提现 | **无接口**,钱出不去 |
| `User.wechatOpenid` | 字段已预留,**无绑定接口**(拿不到 openid) |
| `pom.xml` | 无微信支付 SDK |

**结论**:现在是"假钱闭环"。接真实微信支付 = 开两个口子:**充值(钱进)** + **提现(钱出)**。订单结算不动(继续走余额中转,改动最小)。

---

## 三、接入思路

- 抽象 `PaymentGateway` 接口(发起充值下单 / 处理回调 / 发起提现转账)
- `WechatPayGateway`(真实 V3)+ `MockGateway`(演示 / 无商户号时),配置切换
- `pom` 加官方 SDK `wechatpay-java`
- 订单结算**不动**(继续走平台余额中转)
- 详细接口字段、传输协议、时序图见配套的**《微信支付接口协议.md》**

---

## 四、分阶段计划

### 阶段 1:充值(钱进)- 先做

两前端充值方式不同,后端按 `client` 参数返回不同支付参数:

| 前端 | 支付方式 | 是否需 openid | 后端返回 |
|---|---|---|---|
| H5 | Native 扫码(网页显示二维码,用户微信扫) | 否 | `codeUrl` |
| 小程序 | 小程序支付(JSAPI) | 是 | `payParams`(prepay_id+签名) |

**链路**:发起 -> 微信下单 -> 前端扫码/拉起 -> 用户付款 -> **微信异步回调后端** -> 验签+幂等 -> 加余额 -> 通知前端

**要点**:付款和到账之间有异步回调,前端不能假设点完付款就立即有余额。这步 H5 端不依赖 openid,可先做先联调。

### 阶段 2:绑微信(拿 openid)- 提现前置

| 前端 | 拿 code 方式 | 后端换 openid |
|---|---|---|
| 小程序 | `wx.login` | `jscode2session` |
| H5 | 公众号网页授权 | `oauth2/access_token`(**需公众号 AppID**) |

**要点**:code 怎么拿完全取决于前端形态。小程序端天然能拿 openid;**H5 端需要公众号 AppID** 才能拿(见第五节待确认-4)。

### 阶段 3:提现(钱出)- 依赖绑微信

- `POST /api/user/withdraw` 申请 -> 管理员审核 -> 调商家转账到零钱 -> 状态确认 -> 扣余额
- 审核制,非实时到账。需用户已绑 openid。

---

## 五、前端决策点

### ✅ 已定

1. **前端形态**:H5 + 小程序都要,共用同一套后端

### ⬜ 待确认(定稿前必须回复)

2. **支付成功前端感知方式**:后端 WebSocket 推送 / 前端轮询订单状态?
   - 后端已有 WebSocket 基建(`NotificationWSServer`),推荐 WS 推送;两前端都要能接
3. **提现审核制**(非实时到账):前端能接受这个交互吗?需展示状态流转(申请中/审核中/转账中/成功/失败)
4. **H5 端 openid 来源**:H5 端提现需公众号网页授权拿 openid,**有无公众号 AppID**?若无,H5 端提现引导到小程序端(只做充值)
5. **小程序 AppID / 公众号 AppID**:确认有/无(见第六节,需关联绑定到商户号)
6. **提现并发**:是否限制同一用户同一时间只能有一笔 `applying` 提现(防重复申请)?

---

## 六、AppID 与商户号(材料清单)

组员已准备:**商户号证书 + APIv2 密钥**。但这只是商户号材料,不够。

| 材料 | 用途 | 状态 |
|---|---|---|
| 商户号 mchid | 收付款主体 | ✅ 有 |
| 商户证书 `apiclient_key.pem` | 请求签名 | ✅ 有(组员的 cert.zip) |
| **APIv3 密钥** | 回调 AES 解密 | ⚠️ 待确认(组员准备的是 **APIv2**,V3 接入要用 **APIv3**,不能混用) |
| **小程序 AppID** | 小程序前端 / 小程序支付 / wx.login 拿 openid | ⬜ 待确认 |
| **公众号 AppID** | H5 端网页授权拿 openid | ⬜ 待确认 |

**关键**:
- 小程序 AppID、公众号 AppID、商户号是**三个不同的东西**,要凑齐
- 需在**微信支付商户平台把 AppID 关联绑定到商户号**,才能跑通支付
- V3 接入用 APIv3 密钥,**不是** APIv2(组员那个 APIv2 是老版,不能用于 V3)

---

## 七、联调顺序(按依赖)

```
充值(独立,先做)-> 绑微信(提现前置)-> 提现(依赖绑微信)
```

- H5 端充值(Native 扫码)最独立,不依赖 openid,**建议最先联调**
- 小程序端充值需 openid,可与"绑微信"一起做
- 提现必须先完成绑微信
- 每阶段用 **0.01 元真实小额**测(微信支付无沙箱)
- 两个前端进度可能不同,后端接口 ready 后在群里广播,谁先到谁先联调

---

## 八、建议优先级

```
微信支付充值(H5扫码) > 转账卡片改造 > 绑微信 > 提现
```

H5 充值价值最高且最独立(不依赖 openid、不依赖 AppID),建议先做先联调,跑通"下单-付款-回调-加余额"整条链路。

---

## 附:给前端的一句话

> 前端形态已定:**H5 + 小程序都要,共用我后端**。微信支付分三步:**充值**(H5 扫码 / 小程序拉起付款,钱进平台)、**绑微信**(拿 openid)、**提现**(审核后转账到零钱)。H5 充值最独立,可以先联调。还需要你们定:**(1)支付成功用我后端 WebSocket 推送还是你们轮询?(2)提现是审核制(非实时到账),前端能接受吗?(3)H5 端提现需要公众号 AppID 拿 openid,你们有吗?没有的话 H5 端提现引导到小程序。** 这几个定了我就出接口契约定稿,按 H5充值->绑微信->提现 的顺序联调。详细接口见《微信支付接口协议.md》。
