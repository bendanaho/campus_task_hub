# 微信支付接口协议与交易信息传输协议

> 面向:前端组员(H5 / 小程序)、后端实现依据
> 编写:后端
> 日期:2026-07-15
> 状态:草案(含待确认项,见第八节)

---

## 一、总则

- **适用**:H5 + 小程序两前端,共用同一套后端
- **范围**:充值(钱进)、绑微信(拿 openid)、提现(钱出)。订单结算不动(继续走平台余额中转)
- **基础前缀**:所有接口 `/api` 开头
- **统一响应信封**:`{ "success": bool, "data": T, "errorCode": int, "message": string }`
- **登录态**:除微信回调 `POST /api/pay/notify` 外,所有接口需带 `Authorization: Bearer <jwt>`

---

## 二、交易信息传输协议

### 2.1 前后端传输

| 项 | 约定 |
|---|---|
| 协议 | HTTPS(生产强制) |
| 数据格式 | JSON,UTF-8 |
| 认证 | `Authorization: Bearer <jwt>`(登录态接口) |
| 金额单位 | **元**,数字类型,最多 2 位小数,范围 0.01 ~ 100000 |
| 时间格式 | ISO-8601,如 `2026-07-15T16:00:00` |
| 幂等 | 前端发起充值/提现若超时重试,需用同一 `orderId`/`withdrawId` 查询,不重复发起 |

### 2.2 后端 ↔ 微信支付(V3)传输

| 项 | 约定 |
|---|---|
| 协议 | HTTPS,微信 V3 API |
| 请求签名 | 商户私钥 `apiclient_key.pem`,算法 RSA-SHA256,带 `Authorization: WECHATPAY2-SHA256-RSA2048` 头 |
| 商户证书 | `apiclient_cert.pem` + 证书序列号 |
| 回调验签 | 用**微信支付平台证书**验证回调签名,防伪造 |
| 回调解密 | 回调 body 用 **APIv3 密钥** AES-256-GCM 解密 |
| 金额单位 | V3 用**分**(整数),后端做 元↔分 转换(元×100,四舍五入) |
| 证书/密钥管理 | 商户私钥、APIv3 密钥用**环境变量注入**,**不入仓、不进日志** |

### 2.3 回调传输(微信 → 后端)

- 微信异步回调 `POST /api/pay/notify`,需**公网 HTTPS 可达**
- 后端处理顺序:**验签 → AES 解密 → 取单号 → 幂等校验 → 业务处理 → 返回 SUCCESS**
- 返回给微信:`{"code":"SUCCESS","message":"成功"}`,否则微信会按策略重试(必须幂等)
- **防伪造**:不验签直接处理回调 = 可被伪造充值,禁止

### 2.4 幂等与状态机

- **充值单幂等**:按 `orderId` 查状态,仅 `pending` 才加余额;`paid`/`failed` 收到重复回调直接返回 SUCCESS(不重复加钱)
- **提现单幂等**:按 `withdrawId`,审核通过后只发起一次转账;重复请求返回当前状态

**充值单状态机**:
```
pending ──付款成功──> paid
   │
   └──失败/超时──> failed
```

**提现单状态机**:
```
applying ──审核通过──> approved ──发起转账──> transferring ──成功──> success
   │                                                     │
   └──审核驳回──> rejected                               └──失败──> failed
```

### 2.5 安全要求

1. 商户私钥、APIv3 密钥:环境变量注入,不入仓、不进日志、不返回前端
2. `openid`:返回前端时**脱敏**(如 `oX1***bcd`)
3. 回调:必须验签,禁止跳过
4. 金额:后端用 `BigDecimal`,禁用 `double`(浮点误差)
5. 日志:不打印完整私钥、密钥、支付参数签名;订单号、金额、状态可打

---

## 三、充值接口协议

### 3.1 发起充值 `POST /api/pay/recharge`

**请求:**
```json
{
  "amount": 10.00,
  "client": "h5"
}
```
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| amount | number | 是 | 充值金额,元,0.01~100000,2 位小数 |
| client | string | 是 | `h5` \| `miniapp`,决定返回哪种支付参数 |

**响应:**
```json
{
  "success": true,
  "data": {
    "orderId": "RC20260715160000001",
    "amount": 10.00,
    "status": "pending",
    "payParams": { "...": "按 client 不同" }
  }
}
```

`payParams` 按 `client` 返回不同:
- **h5**(Native 扫码):
```json
{ "codeUrl": "weixin://wxpay/bizpayurl?pr=xxx" }
```
- **miniapp**(小程序支付):
```json
{
  "appId": "wxXXX",
  "timeStamp": "1721036800",
  "nonceStr": "xxx",
  "package": "prepay_id=wx20260715xxx",
  "signType": "RSA",
  "paySign": "xxx"
}
```

**前端职责:**
- h5:用 `codeUrl` 生成二维码展示,用户用微信扫码付款
- miniapp:用 `payParams` 调 `wx.requestPayment` 拉起支付

### 3.2 查询充值订单 `GET /api/pay/recharge/{orderId}`

**响应:**
```json
{
  "data": {
    "orderId": "RC20260715160000001",
    "amount": 10.00,
    "status": "paid",
    "paidAt": "2026-07-15T16:01:00",
    "balance": 110.00
  }
}
```
| 字段 | 说明 |
|---|---|
| status | `pending` \| `paid` \| `failed` |
| balance | `paid` 时返回最新余额,其他状态可不返回 |

### 3.3 微信回调 `POST /api/pay/notify`

- **前端不直接调**,但需知道这条链路存在
- 微信 V3 回调格式(签名头 + AES 加密 body)
- 后端处理:验签 → 解密 → 取 `out_trade_no`/`transaction_id`/金额 → 幂等 → 置 `paid` → `balance += amount` → 记 `in` 流水(`category=recharge`)→ 推送前端

### 3.4 支付成功通知前端

**推荐:WebSocket 推送**(后端回调成功后推给用户):
```json
{ "type": "PAY_SUCCESS", "orderId": "RC...", "amount": 10.00, "balance": 110.00 }
```
**备选:前端轮询** `GET /api/pay/recharge/{orderId}`,每 2s 一次,`status=paid` 停止。

> ⚠️ 待确认(第八节-1):通知方式选 WS 还是轮询,两前端都要能接。

### 3.5 充值时序

```
前端                后端                微信支付
 │                  │                    │
 │ POST /recharge   │                    │
 │{amount,client}──>│                    │
 │                  │ 创建 pending 单    │
 │                  │ 调 Native/JSAPI 下单 ──>│
 │                  │ <──── code_url/prepay_id ──│
 │ <── payParams ──│                    │
 │                  │                    │
 │ 展示二维码/拉起支付                   │
 │ 用户付款 ──────────────────────────>│
 │                  │                    │
 │                  │ <── POST /notify(验签+解密) ──│
 │                  │ 加余额+记流水      │
 │                  │                    │
 │ <── WS PAY_SUCCESS ─│                 │
 │ 刷新余额          │                    │
```

---

## 四、绑微信接口协议(拿 openid,提现前置)

### 4.1 绑定 `POST /api/auth/wechat/bind`

**请求:**
```json
{
  "code": "061Xxx...",
  "client": "miniapp"
}
```
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| code | string | 是 | `wx.login`(小程序)或公众号网页授权(H5)拿的 code |
| client | string | 是 | `miniapp` \| `official` |

**后端处理:**
- `miniapp`:调 `jscode2session` 换 openid
- `official`:调 `oauth2/access_token` 换 openid
- 存入 `User.wechatOpenid`

**响应:**
```json
{ "data": { "bound": true, "openid": "oX1***bcd" } }
```

### 4.2 查询绑定状态 `GET /api/user/wechat/bound`

**响应:**
```json
{ "data": { "bound": true } }
```

> ⚠️ 待确认(第八节-2):H5 端用公众号授权拿 openid,需**公众号 AppID**;若无,H5 端提现引导到小程序端。

---

## 五、提现接口协议

### 5.1 申请提现 `POST /api/user/withdraw`

**请求:**
```json
{ "amount": 10.00 }
```

**校验:** 已绑 openid、余额 ≥ amount、金额范围 0.01~100000、2 位小数

**响应:**
```json
{
  "data": {
    "withdrawId": "WD20260715160000001",
    "amount": 10.00,
    "status": "applying",
    "createdAt": "2026-07-15T16:00:00"
  }
}
```

> 注:申请时**不扣余额**,审核通过转账成功后才扣(避免审核驳回还要退款)。

### 5.2 查询提现 `GET /api/user/withdraw/{id}`

**响应:**
```json
{
  "data": {
    "withdrawId": "WD...",
    "amount": 10.00,
    "status": "success",
    "reason": "",
    "createdAt": "...",
    "finishedAt": "..."
  }
}
```
| status | 含义 | 前端展示 |
|---|---|---|
| applying | 待审核 | 申请中 |
| approved | 审核通过,转账中 | 转账中 |
| transferring | 已发起转账 | 转账中 |
| success | 到账成功 | 已到账 |
| failed | 转账失败 | 提现失败 |
| rejected | 审核驳回 | 已驳回(reason 给原因) |

### 5.3 管理员审核(后端已有 admin 框架)

- `POST /api/admin/withdraw/{id}/approve`:置 `approved` → 调商家转账到零钱(`/v3/transfer/batches`)→ `transferring` → 查询/回调确认 → `success` 扣余额 + 记 `out` 流水 + WS 通知
- `POST /api/admin/withdraw/{id}/reject`:置 `rejected`,带 reason

### 5.4 提现时序

```
前端                后端                管理员           微信支付
 │ POST /withdraw  │                    │                 │
 │{amount}────────>│ 建 applying 单    │                 │
 │ <── withdrawId ─│                    │                 │
 │                  │                    │                 │
 │                  │ <── 审核 approve ──│                 │
 │                  │ 调商家转账 ────────────────────────>│
 │                  │ transferring      │                 │
 │                  │ <── 转账结果(查询/回调) ───────────│
 │                  │ success: 扣余额+记流水              │
 │ <── WS 通知 ─────│                    │                 │
```

> ⚠️ 待确认(第八节-3):提现审核制非实时到账,前端交互/文案需适配。

---

## 六、错误码

| errorCode | 含义 | 触发场景 |
|---|---|---|
| 1001 | 参数错误 | 金额非法、client 非法、code 为空等 |
| AUTH_REQUIRED | 未登录 | 缺/失效 token |
| BALANCE_NOT_ENOUGH | 余额不足 | 提现金额 > 余额 |
| WECHAT_NOT_BOUND | 未绑定微信 | 提现时无 openid |
| RECHARGE_AMOUNT_INVALID | 充值金额非法 | amount 越界/小数位过多 |
| ORDER_ALREADY_PAID | 订单已支付 | 重复支付 |
| PAY_FAILED | 支付/转账失败 | 微信返回失败 |
| WITHDRAW_PENDING | 已有提现审核中 | 防并发提现(可选) |

> 新增错误码需同步到 `ErrorCode.java` 枚举 + 前端错误处理。

---

## 七、数据约定汇总

| 项 | 前后端 | 后端↔微信 |
|---|---|---|
| 金额单位 | 元(BigDecimal) | 分(整数) |
| 小数位 | 2 位 | - |
| 时间 | ISO-8601 | Unix 时间戳(秒) |
| 数据格式 | JSON | JSON |
| 认证 | JWT | 商户证书签名 |
| 回调 | - | 平台证书验签 + APIv3 密钥 AES 解密 |

---

## 八、待确认项(定稿前必须回复)

1. **支付成功通知方式**:WebSocket 推送(推荐)还是前端轮询?两前端都要能接。
2. **H5 端 openid 来源**:H5 端提现需公众号网页授权拿 openid,**有无公众号 AppID**?若无,H5 端提现引导到小程序端。
3. **小程序 AppID / 公众号 AppID**:确认有/无。需在微信支付商户平台把 AppID 关联绑定到商户号。
4. **提现审核制**:非实时到账,前端能接受吗?
5. **提现并发**:是否限制同一用户同一时间只能有一笔 applying 提现(防重复申请)?

---

## 九、接口清单(速查)

| 方法 | 路径 | 登录 | 说明 |
|---|---|---|---|
| POST | /api/pay/recharge | 是 | 发起充值,返回支付参数 |
| GET | /api/pay/recharge/{orderId} | 是 | 查充值订单状态 |
| POST | /api/pay/notify | 否 | 微信回调(前端不调) |
| POST | /api/auth/wechat/bind | 是 | code 换 openid 绑定 |
| GET | /api/user/wechat/bound | 是 | 查绑定状态 |
| POST | /api/user/withdraw | 是 | 申请提现 |
| GET | /api/user/withdraw/{id} | 是 | 查提现状态 |
| POST | /api/admin/withdraw/{id}/approve | 管理员 | 审核通过+转账 |
| POST | /api/admin/withdraw/{id}/reject | 管理员 | 审核驳回 |
