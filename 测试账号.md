# 校园互助平台 · 测试账号

> 给组员测试用。账号密码统一、数据已预置，直接登录即可测全部功能。
>
> 数据来源：`db/test-data.sql`（prod / MySQL 环境）+ `db/add-test-data.sql`（额外带图任务）

---

## 访问地址

**http://182.92.133.163:8081**

登录页：`http://182.92.133.163:8081/login.html`

---

## 测试账号一览

| 用户名 | 密码 | 角色 | 实名状态 | 信用分 | 余额 | 真实姓名 | 学院 | 主要用途 |
|---|---|---|---|---|---|---|---|---|
| `xiaoming` | `123456` | 普通用户 | ✅ 已实名 | 4.8 | 100.00 | 张三 | 计算机学院 | 主测发帖、接单、支付 |
| `xiaohong` | `123456` | 普通用户 | ✅ 已实名 | 5.0 | 50.50 | 李四 | 电子工程学院 | 配合交易、接单 |
| `xiaowang` | `123456` | 普通用户 | ❌ 未实名 | 3.5 | 0.00 | — | — | 测实名门禁 |
| `admin` | `123456` | 管理员 | ✅ 已实名 | 5.0 | 9999.99 | 管理员 | — | 管理后台、仲裁、举报 |

> 所有账号密码均为 `123456`。手机号均为 139 段（1390013900x），邮箱见 test-data.sql。

---

## 各账号测试场景建议

### `xiaoming`（张三，已实名，余额 100）-- 主测试账号
- 发各种类型的帖子（悬赏 payer / 服务 earner / 互助 none）
- 接别人的单、支付报酬
- 发起聊天、WebSocket 实时消息
- 评价对方、查看信用分变化
- 查看交易流水、账户余额

### `xiaohong`（李四，已实名，余额 50.5）-- 配合账号
- 和 xiaoming 互相接单、交易
- 测双人都确认完成 -> 订单完成 -> 互相评价流程
- 测支付/转账卡片消息

### `xiaowang`（未实名，余额 0）-- 实名门禁
- 登录后测**未实名限制**（如不能发帖/接单，引导去实名认证）
- 完成实名认证后，验证权限解锁
- 余额 0，可测充值或接单 earning 流程

### `admin`（管理员）-- 后台功能
- **争议仲裁**：对 disputed 状态的订单做裁决（退款/调解/部分退款 -> closed）
- **帖子管理**：下架/删除违规帖子
- **举报处理**：查看用户举报，按帖聚合处理
- **只读取证**：查看争议订单的证据（聊天、图片），不直接干预交易

---

## 预置数据概况

| 数据 | 数量 | 说明 |
|---|---|---|
| 用户 | 4 | 上表 4 个账号 |
| 任务/帖子 | 14 | test-data 4 个 + add-test-data 10 个（7 个带图） |
| 订单 | 2 | 1 个 completed，1 个 in_progress |
| 会话 | 4 | 含 2 个系统通知会话 |
| 聊天消息 | 6 | 含文字、系统、付款卡片 |
| 评价 | 见 test-data | 含默认好评 |

> 任务大厅（`task-hall.html`）能看到 14 个任务，其中 7 个带缩略图，点进详情看大图。

---

## 功能测试清单

### 基础流程
- [ ] 登录（4 个账号都能登）
- [ ] 注册新账号
- [ ] 实名认证（用 xiaowang 测未实名 -> 实名）
- [ ] 浏览任务大厅、筛选分类
- [ ] 查看任务详情（带图任务看图片显示）

### 交易流程
- [ ] 发帖（payer/earner/none 三种）
- [ ] 接单 -> 支付 -> 双方确认完成 -> 评价
- [ ] 信用分变化（评价后贝叶斯重算）
- [ ] 交易流水、余额变动

### 聊天 / 实时
- [ ] 开两个浏览器（如 xiaoming + xiaohong），互发消息**实时收到**（WebSocket）
- [ ] 付款卡片消息
- [ ] 系统通知（订单状态变更推送）

### 争议仲裁（用 admin）
- [ ] 普通用户发起争议 -> 订单进 disputed
- [ ] admin 仲裁：退款 / 调解 / 部分退款 -> closed
- [ ] 查看争议证据

### 管理后台（用 admin）
- [ ] 帖子下架/删除
- [ ] 举报处理
- [ ] 只读取证

### 自动确认
- [ ] in_progress 订单超过 2 天自动确认完成（可调时间或等）

---

## 注意事项

1. **数据在 MySQL 持久化**，你的操作会保留。请勿恶意删除他人测试数据。
2. **WebSocket 实时测试**：开两个浏览器（或一个隐身窗口），分别登录不同账号，发消息应实时收到。
3. **图片显示**：带图任务的图片是 picsum 外链，浏览器需联网；加载失败会自动隐藏，不影响功能。
4. **重置数据**：如需恢复初始状态，重新导入：
   ```bash
   mysql -uroot -p campus_task_hub < /root/campus/test-data.sql
   mysql -uroot -p campus_task_hub < /root/campus/add-test-data.sql
   ```
   （注意 test-data.sql 用的是 `INSERT INTO ... VALUES`，重导前可能需先清空相关表，或用 TRUNCATE）
5. **服务器重启后**：若 8081 打不开，跑 `iptables -I INPUT 7 -p tcp --dport 8081 -j ACCEPT` 放行。

---

## 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 登录提示"用户不存在" | 用户名拼错，或数据库被清空（重导 test-data.sql） |
| 页面能开但数据加载失败 | 后端没起，联系部署人 `systemctl status campus-backend` |
| 聊天不实时 | WebSocket 没连上，F12 Network -> WS 看连接；或后端/Nginx 的 /ws 反代问题 |
| 图片不显示 | picsum 外链网络问题，onerror 自动隐藏；不影响功能 |
| 8081 打不开 | iptables 8081 放行掉了（服务器重启过），重跑放行命令 |
