# 校园互助平台 · 部署指南

> 面向：负责部署的组员。把项目部署到远程服务器，用 `IP:端口` 访问（域名 `campustaskhub.cn` 备案后再切）。
>
> 本指南已与 **2026-07 最新代码**逐条核对，修正了旧版《部署速查单》中 4 处与代码不符的地方（前端 localhost、schema 路径、prod 空库、演示数据导入）。
>
> 标记：**[本机]** = 在你自己电脑上做；**[服务器]** = SSH 登录服务器后做。

---

## 0. 核心原理（先读懂再动手）

- **前端 JS 跑在用户浏览器里**，所以不能写死 `localhost:8080`。当前代码已改成**环境自适应**：本地开发自动连 `localhost:8080`，部署后自动走同源相对路径 `/api`、`/ws`，由 Nginx 反代到本机 8080 后端。**部署时无需手动改前端任何配置。**
- 后端对外只暴露 8081（Nginx），8080 只监听 `127.0.0.1`，不直接对外。
- prod profile 下 `DataInitializer` **不运行**（`@Profile("!prod")`），所以 prod + MySQL 启动后库是空的，必须手动导入演示数据，否则无法登录。详见第 5 步。

---

## 1. 服务器信息

| 项 | 值 |
|---|---|
| 公网 IP | `182.92.133.163` |
| 登录用户 | `root` |
| 密码 | **私发给你**（勿贴到仓库 / 大群 / 文档） |
| 系统 | Alibaba Cloud Linux 3（RHEL 系，用 `dnf`/`yum`） |
| 已装 | JDK 17、Nginx、MariaDB（未启动）、Docker |
| 已放行端口 | 22 / 80 / 443 / 8081（8081 是主机 iptables 临时放行，**重启会掉**，见附 1） |

```bash
ssh root@182.92.133.163      # 回车后输入密码
```
> 这台机器还跑着别的站点（yuniteam、fushizhishiku），**只新增文件、不要动别人已有的 Nginx 配置**。

---

## 2. [本机] 前置工具

- **Git**、**JDK 17**（打包后端必需，`java -version` 要能看到 17）。
- Maven 不用单独装，仓库自带 `mvnw`。
- 一个 SFTP 工具：FileZilla（主机 `sftp://182.92.133.163`、用户 root）或系统自带 `scp`。

---

## 3. [本机] 拉代码（前后端在同一仓库的不同分支）

```bash
# 前端（front-end 分支，含 front-end/ 目录）
git clone -b front-end https://github.com/bendanaho/campus_task_hub.git campus-frontend

# 后端（back-end 分支，含 Keshe_backend/ 和 db/）
git clone -b back-end  https://github.com/bendanaho/campus_task_hub.git campus-backend
```

---

## 4. [本机] 前端配置（已无需手动改，仅说明）

旧版手册要求改 `api.js` 和 `main.js` 里的 `localhost:8080`。**当前代码已改成环境自适应，跳过此步即可：**

- `front-end/js/api.js`：`API_BASE` 根据当前页面 hostname 自动切换
  - `localhost` / `127.0.0.1` / `file://` → `http://localhost:8080/api`（本地开发）
  - 其他（IP / 域名）→ `/api`（部署，走 Nginx 反代）
- `front-end/js/main.js`（搜索 `new WebSocket`）：同理，部署后自动用 `ws(s)://<当前host>/ws/notification/...`

> 如果你拉到的代码里这两处仍是写死的 `localhost:8080`，说明分支不是最新的，`git pull` 一下。

---

## 5. [本机] 打包后端 jar

```bash
cd campus-backend/Keshe_backend
mvnw.cmd clean package -DskipTests          # Windows CMD/PowerShell
# Git Bash 用户改用：  ./mvnw clean package -DskipTests
ls target/*.jar                              # 得到 Keshe_backend-0.0.1-SNAPSHOT.jar
```

> ⚠️ 打包前先确认 `src/main/resources/application-prod.yml` 里的数据库 `password` 是**服务器 MariaDB 的密码**（不是本机开发密码）。详见第 7 步。若不想把密码写进文件，可跳过此处，启动时用 `-Dspring.datasource.password=xxx` 注入。

---

## 6. [本机] 上传文件到服务器

先在服务器建目录（一次性）：
```bash
ssh root@182.92.133.163 "mkdir -p /root/campus /var/www/campus-task-hub"
```

上传 4 类文件：
```bash
# 后端 jar
scp Keshe_backend/target/Keshe_backend-0.0.1-SNAPSHOT.jar root@182.92.133.163:/root/campus/app.jar

# 数据库脚本（注意：在 campus-backend/ 根目录下执行，路径是 db/xxx.sql）
scp db/schema.sql     root@182.92.133.163:/root/campus/schema.sql
scp db/test-data.sql  root@182.92.133.163:/root/campus/test-data.sql   # 课设演示数据，见第 7 步

# 前端静态文件
scp -r campus-frontend/front-end/* root@182.92.133.163:/var/www/campus-task-hub/
```

> 旧手册写的 `scp ../schema.sql` 路径已失效，实际脚本在 `db/` 目录下。

---

## 7. [服务器] 数据库（三选一）

### 方案 A — 快速演示（内置 H2，零数据库配置）

第 8 步 systemd 启动命令里**去掉** `--spring.profiles.active=prod` 即可。
- 默认 profile 用 H2 内存库 + `ddl-auto=update` + `DataInitializer` 自动灌种子数据（8 用户 / 22 帖子 / 6 订单…，账号密码统一 `1`，admin 也是 `1`）。
- 优点：零配置。缺点：**重启后端数据清空**，回到种子数据（演示够用）。

### 方案 B — 课设演示/验收（MariaDB，数据持久）✅ 推荐

```bash
systemctl enable --now mariadb
mysql_secure_installation                 # 按提示设 root 密码，记下来（第 8 步要用）

# schema.sql 自带 CREATE DATABASE / USE，直接导入即可
mysql -uroot -p < /root/campus/schema.sql

# ⚠️ 关键：prod 下 DataInitializer 不跑，库是空的，必须导入演示数据才能登录！
mysql -uroot -p < /root/campus/test-data.sql
```

导入后可用账号（密码均为 `123456`）：

| 用户名 | 密码 | 角色 | 说明 |
|---|---|---|---|
| `xiaoming` | `123456` | 普通用户 | 已实名，信用 4.8 |
| `xiaohong` | `123456` | 普通用户 | 已实名，信用 5.0 |
| `xiaowang` | `123456` | 普通用户 | 未实名（可测实名门禁） |
| `admin` | `123456` | 管理员 | 仲裁/帖子管理/举报处理 |

### 方案 C — 真实上线（MariaDB，库为空，用户自行注册）

```bash
systemctl enable --now mariadb
mysql_secure_installation
mysql -uroot -p < /root/campus/schema.sql
# 不导 test-data.sql，保持库为空

# 只建一个初始管理员（用于仲裁/管理），需先手填 BCrypt 哈希：
#   编辑 /root/campus/ 下放一份 db/seed/admin.sql，把 '<填入BCrypt哈希>' 换成
#   明文密码的 BCrypt 哈希（dev 启动后到 /h2-console 查 users.password_hash 可得）
mysql -uroot -p campus_task_hub < /root/campus/admin.sql
```
> 上线后普通用户走注册流程，第一个管理员由 `admin.sql` 创建。

### 无论 B/C：改后端数据库密码

`application-prod.yml` 当前 `password` 是**本机开发密码**，部署前必须改成你刚设的服务器 MariaDB 密码，然后重新打包上传（第 5 步）。或启动时注入避免改文件：

```bash
# 方式1：改 application-prod.yml 的 password 后重新打包（密码会进 jar，不推荐）
# 方式2：systemd 启动参数注入（推荐，见第 8 步 ExecStart 追加 -Dspring.datasource.password=xxx）
```

> `application-prod.yml` 的 JDBC URL 已是 `useSSL=false&allowPublicKeyRetrieval=true`，无需再改（旧手册这条已过时）。
> `ddl-auto=validate`：只校验不建表，所以 `schema.sql` 必须先导入，且列结构必须与实体一致（已对齐，含 `reports` 举报表）。

---

## 8. [服务器] 后端配成 systemd 服务

```bash
cat > /etc/systemd/system/campus-backend.service <<'EOF'
[Unit]
Description=Campus Task Hub Backend
After=network.target

[Service]
User=root
WorkingDirectory=/root/campus
# 方案A(H2)：删掉 --spring.profiles.active=prod
# 方案B/C(MySQL)：保留 prod；如不在 yml 里写密码，追加 -Dspring.datasource.password=你的库密码
ExecStart=/usr/bin/java -jar /root/campus/app.jar --spring.profiles.active=prod --server.address=127.0.0.1
SuccessExitStatus=143
Restart=on-failure

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable --now campus-backend
systemctl status campus-backend --no-pager            # 看是否 active (running)
curl -s http://127.0.0.1:8080/api/posts | head -c 200 # 本机验证后端通
```
> `--server.address=127.0.0.1` 让后端只监听本机，由 Nginx 代理，更安全。
> 看日志：`journalctl -u campus-backend -f`；看报错：`journalctl -u campus-backend -e`

---

## 9. [服务器] Nginx（静态前端 + 反代 /api 和 /ws）

```bash
cat > /etc/nginx/conf.d/campus-task-hub.conf <<'EOF'
server {
    listen 8081;              # 测试端口；接域名后改成 80 并加 server_name
    server_name _;

    root /var/www/campus-task-hub;
    index login.html index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /ws/ {
        proxy_pass http://127.0.0.1:8080/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 300s;
    }
}
EOF

nginx -t && systemctl reload nginx
```

---

## 10. 验证

浏览器打开 **http://182.92.133.163:8081** → 能看到登录页。

- 方案 A：用 `张三` / `1`（或 `admin` / `1`）登录。
- 方案 B：用 `admin` / `123456` 登录，浏览大厅、发帖、聊天（WebSocket 实时）、管理员仲裁。
- 方案 C：用 `admin.sql` 设的账号登录后台。

> 本机若开了代理 / VPN（Clash 等），访问纯 IP 可能被代理拦截：把该 IP 设为直连，或临时关代理，或用手机流量访问验证。

---

## 附 1：端口 / 防火墙（这台机器的坑）

主机层 iptables 默认只放行 22/80/443，**其它端口 REJECT**。8081 是临时加的放行规则，**服务器重启后会消失**：
```bash
# 立即放行（若重启后 8081 又不通，重跑这条）
iptables -I INPUT 7 -p tcp --dport 8081 -j ACCEPT
```
安全组（阿里云控制台）里 8081 已放行，不用再动。

## 附 2：域名 `campustaskhub.cn` 备案通过后

前端已用相对路径，**不用改代码**：
1. 云解析加 A 记录 → `182.92.133.163`；
2. 把第 9 步的 `listen 8081;` 改成 `listen 80;`，加 `server_name campustaskhub.cn www.campustaskhub.cn;`（80 已放行）；`nginx -t && systemctl reload nginx`；
3. HTTPS：`dnf install -y certbot python3-certbot-nginx && certbot --nginx -d campustaskhub.cn -d www.campustaskhub.cn`（自动配 443，前端 WebSocket 会自动切 wss）。

## 附 3：常见问题速查

| 现象 | 排查 |
|---|---|
| 页面能开但登录提示错误 / 401 | 后端没起：`systemctl status campus-backend`；本机 `curl 127.0.0.1:8080/api/posts` |
| **方案 B 登录提示"用户不存在"** | **忘了导 test-data.sql**：`mysql -uroot -p < /root/campus/test-data.sql`（prod 下种子数据不自动生成） |
| 后端起不来，日志报 validate 失败 | schema.sql 没导或版本旧：`mysql -uroot -p < /root/campus/schema.sql` 重新导；确认含 `reports` 表 |
| 后端起不来，日志报 Access denied | MariaDB 密码不对：改 `application-prod.yml` 或 systemd 启动参数里的 `spring.datasource.password` |
| 8081 打不开、TCP 连不上 | iptables 8081 放行掉了（重跑附 1 命令）；或本机代理拦截 |
| 静态页 404 | `/var/www/campus-task-hub/` 里要有 `login.html`，确认 `scp -r front-end/*` 上传对了 |
| 聊天不实时 | WebSocket：确认 Nginx 有 `/ws/` 那段（含 Upgrade 头）；浏览器 F12 Network → WS 看连接状态 |
| 部署后前端仍连 localhost:8080 | 拉到的 front-end 分支不是最新：`cd campus-frontend && git pull` 重新上传 |

## 附 4：数据策略对照

| 场景 | profile | 数据库 | 种子数据来源 | 账号 |
|---|---|---|---|---|
| 本地开发 | 默认（无 prod） | H2 内存 | `DataInitializer` 自动 | 8 用户，密码 `1` |
| 方案 A 快速演示 | 默认（无 prod） | H2 内存 | `DataInitializer` 自动 | 同上，重启清空 |
| 方案 B 课设验收 | prod | MariaDB | 导 `test-data.sql` | 4 账号，密码 `123456` |
| 方案 C 真实上线 | prod | MariaDB | 仅 `seed/admin.sql` 建管理员 | admin 自定，其余注册 |

## 附 5：安全提醒

- root 密码、数据库密码只私发、别外泄、别提交到仓库（本仓库是**公开仓库**，任何明文密码进 commit 都等于公开）。
- `application-prod.yml` 已在 `.gitignore` 中，本地改密码不会被提交；若用 systemd 启动参数注入密码，注意 `journalctl` 日志可能含参数，权限别开放给无关人员。
- 更稳的做法：让每个组员把自己的 **SSH 公钥**加到 `~/.ssh/authorized_keys`，就不用共享 root 密码。
