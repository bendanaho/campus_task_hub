# 校园互助平台 · 部署指南

> 面向：负责部署的组员。把项目部署到远程服务器，用 `IP:端口` 访问（域名 `campustaskhub.cn` 备案后再切）。
>
> 本指南基于 **2026-07-12 实际部署验证**，反映服务器真实环境（宝塔 MySQL 5.7）。已踩过的坑都标注了。
>
> 标记：**[本机]** = 在你自己电脑上做；**[服务器]** = SSH 登录服务器后做。

---

## 0. 核心原理（先读懂再动手）

- **前端 JS 跑在用户浏览器里**，不能写死 `localhost:8080`。当前代码已改成**环境自适应**：本地开发自动连 `localhost:8080`，部署后自动走同源相对路径 `/api`、`/ws`，由 Nginx 反代到本机 8080 后端。**部署时无需手动改前端任何配置。**
- 后端对外只暴露 8081（Nginx），8080 只监听 `127.0.0.1`，不直接对外。
- prod profile 下 `DataInitializer` **不运行**（`@Profile("!prod")`），所以 prod + MySQL 启动后库是空的，必须手动导入 `test-data.sql` 才能登录。

---

## 1. 服务器信息

| 项 | 值 |
|---|---|
| 公网 IP | `182.92.133.163` |
| 登录用户 | `root` |
| 密码 | **私发给你**（勿贴到仓库 / 大群 / 文档） |
| 系统 | Alibaba Cloud Linux 3（RHEL 系，用 `dnf`/`yum`） |
| 数据库 | **宝塔 MySQL 5.7**（数据在 `/www/server/data`，`/etc/my.cnf` 是宝塔配置） |
| 已装 | JDK 17、Nginx、宝塔 MySQL 5.7 |
| 已放行端口 | 22 / 80 / 443 / 8081（8081 是主机 iptables 临时放行，**重启会掉**，见附 1） |

```bash
ssh root@182.92.133.163      # 回车后输入密码
```

> ⚠️ **这台机器是宝塔环境，三个雷区别踩**：
> 1. `/etc/my.cnf` 是宝塔配置（`datadir=/www/server/data` 等），**别改**。
> 2. `/www/server/data` 是宝塔数据目录，**别删/别覆盖**（可能含别的库）。
> 3. **别 `dnf install mariadb-server`**：MariaDB 10.5 会读宝塔的 `/etc/my.cnf`，因版本不兼容（不能升级 5.7 数据）+ 不认 `early-plugin-load=""` 而**启动失败**（实测踩坑）。要用数据库直接用宝塔 5.7。

---

## 2. [本机] 前置工具

- **Git**、**JDK 17**（`java -version` 要能看到 17）。
- Maven 不用单独装，仓库自带 `mvnw`。
- SFTP 工具：WindTerm（拖拽上传）、FileZilla 或 `scp`。

---

## 3. [本机] 拉代码（前后端在同一仓库的不同分支）

```bash
git clone -b front-end https://github.com/bendanaho/campus_task_hub.git campus-frontend
git clone -b back-end  https://github.com/bendanaho/campus_task_hub.git campus-backend
```

---

## 4. [本机] 前端配置（已无需手动改，仅说明）

当前代码已改成环境自适应，**跳过此步**：
- `front-end/js/api.js`：`API_BASE` 按 hostname 自动切（本地 `localhost:8080/api`，部署 `/api`）
- `front-end/js/main.js`（搜 `new WebSocket`）：同理，部署后自动 `ws(s)://<host>/ws/...`

> 若拉到的代码这两处仍是写死 `localhost:8080`，说明分支不是最新，`git pull`。

---

## 5. [本机] 打包后端 jar

```bash
cd campus-backend/Keshe_backend
mvnw.cmd clean package -DskipTests          # Windows CMD/PowerShell
# Git Bash：  ./mvnw clean package -DskipTests
ls target/*.jar                              # Keshe_backend-0.0.1-SNAPSHOT.jar
```

> 打包前**不用改** `application-prod.yml` 的密码（里面是本机开发密码）。部署时用环境变量文件覆盖（第 8 步），密码不进 jar。

---

## 6. [本机] 上传文件到服务器

先建目录（一次性）：
```bash
ssh root@182.92.133.163 "mkdir -p /root/campus /var/www/campus-task-hub"
```

用 WindTerm 拖拽或 `scp` 上传：

| 本地文件 | 服务器路径 | 备注 |
|---|---|---|
| `Keshe_backend/target/Keshe_backend-0.0.1-SNAPSHOT.jar` | `/root/campus/app.jar` | 上传后重命名 app.jar |
| `db/schema.sql`、`db/test-data.sql` | `/root/campus/` | |
| `db/add-test-data.sql`（可选，带图任务） | `/root/campus/` | |
| `campus-frontend/front-end/` 里的内容 | `/var/www/campus-task-hub/` | 文件直接放，别多一层 front-end |

---

## 7. [服务器] 数据库（三选一）

### 方案 A - 快速验证（内置 H2，零数据库配置）

第 8 步 systemd 启动命令里**去掉** `--spring.profiles.active=prod` 即可。
- 默认 profile 用 H2 内存库 + `DataInitializer` 自动灌种子（8 用户 / 22 帖子…，账号密码统一 `1`，admin 也是 `1`）。
- 优点：零配置。缺点：**重启后端数据清空**。仅用于快速验证链路。

### 方案 B - 课设演示/验收（宝塔 MySQL 5.7，数据持久）✅ 推荐

**1) 启动宝塔 5.7**（如果没启动）：
```bash
/etc/init.d/mysqld start
ss -tlnp | grep 3306          # 确认 3306 在监听，users:(("mysqld",...))
```

**2) 如果不知道 root 密码，重置**（跳过授权表）：
```bash
# 停 MySQL
/etc/init.d/mysqld stop

# 跳过授权表启动
/www/server/mysql/bin/mysqld_safe --skip-grant-tables --skip-networking --user=mysql --datadir=/www/server/data &
sleep 4

# 改密码（把 你的新密码 换成你要设的，需大小写+数字+特殊字符+8位）
mysql -uroot -e "FLUSH PRIVILEGES; ALTER USER 'root'@'localhost' IDENTIFIED BY '你的新密码'; FLUSH PRIVILEGES;"

# 关掉无密码模式，正常启动
pkill -f 'skip-grant-tables'; sleep 3
/etc/init.d/mysqld start

# 验证
mysql -uroot -p'你的新密码' -e "SELECT VERSION();"     # 返回 5.7.40-log 即成功
```

**3) 导入数据**（schema 自带建库，test-data 是演示数据）：
```bash
mysql -uroot -p < /root/campus/schema.sql
mysql -uroot -p < /root/campus/test-data.sql      # ⚠️ 必须导！prod 下种子数据不自动生成
mysql -uroot -p < /root/campus/add-test-data.sql  # 可选：10 个带图任务
```

**4) 验证**（8 表 + 4 用户）：
```bash
mysql -uroot -p -e "USE campus_task_hub; SHOW TABLES; SELECT id,username,role FROM users;"
```

导入后账号（密码均为 `123456`）：

| 用户名 | 密码 | 角色 | 说明 |
|---|---|---|---|
| `xiaoming` | `123456` | 普通用户 | 已实名，信用 4.8，余额 100 |
| `xiaohong` | `123456` | 普通用户 | 已实名，信用 5.0，余额 50.5 |
| `xiaowang` | `123456` | 普通用户 | 未实名（测实名门禁） |
| `admin` | `123456` | 管理员 | 仲裁/帖子管理/举报处理 |

### 方案 C - 真实上线（宝塔 5.7 空库，用户自行注册）

```bash
mysql -uroot -p < /root/campus/schema.sql       # 只建表，不导 test-data
# 用 db/seed/admin.sql 建初始管理员（需先手填 BCrypt 哈希，见该文件注释）
mysql -uroot -p campus_task_hub < /root/campus/admin.sql
```
> 上线后普通用户走注册，第一个管理员由 `admin.sql` 创建。

---

## 8. [服务器] 后端配成 systemd 服务（prod + 环境变量文件存密码）

**1) 密码存环境变量文件**（不暴露在命令行/日志）：
```bash
printf 'SPRING_DATASOURCE_PASSWORD=你的库密码\n' > /etc/campus-backend.env
chmod 600 /etc/campus-backend.env
```

**2) 写 systemd 配置**（用 `printf` 一行写，避免 heredoc 缩进问题）：
```bash
printf '%s\n' '[Unit]' 'Description=Campus Task Hub Backend (MySQL prod)' 'After=network.target' '' '[Service]' 'User=root' 'WorkingDirectory=/root/campus' 'EnvironmentFile=/etc/campus-backend.env' 'ExecStart=/usr/bin/java -jar /root/campus/app.jar --spring.profiles.active=prod --server.address=127.0.0.1' 'SuccessExitStatus=143' 'Restart=on-failure' '' '[Install]' 'WantedBy=multi-user.target' > /etc/systemd/system/campus-backend.service
```

> Spring Boot 环境变量 `SPRING_DATASOURCE_PASSWORD` 自动映射 `spring.datasource.password`，覆盖 `application-prod.yml` 里的本机密码。URL（`localhost:3306`）和 username（root）已匹配宝塔 5.7，无需改。

**3) 启动 + 验证**：
```bash
systemctl daemon-reload && systemctl enable --now campus-backend
sleep 15
systemctl status campus-backend --no-pager | head -8            # active (running)
curl -s http://127.0.0.1:8080/api/posts | head -c 200           # 返回帖子 JSON
```
> `--server.address=127.0.0.1` 让后端只监听本机，由 Nginx 代理。
> 看日志：`journalctl -u campus-backend -f`

---

## 9. [服务器] Nginx（静态前端 + 反代 /api 和 /ws）

```bash
printf '%s\n' 'server {' '    listen 8081;' '    server_name _;' '' '    root /var/www/campus-task-hub;' '    index login.html index.html;' '' '    location / {' '        try_files $uri $uri/ =404;' '    }' '' '    location /api/ {' '        proxy_pass http://127.0.0.1:8080/api/;' '        proxy_set_header Host $host;' '        proxy_set_header X-Real-IP $remote_addr;' '        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;' '    }' '' '    location /ws/ {' '        proxy_pass http://127.0.0.1:8080/ws/;' '        proxy_http_version 1.1;' '        proxy_set_header Upgrade $http_upgrade;' '        proxy_set_header Connection "upgrade";' '        proxy_set_header Host $host;' '        proxy_read_timeout 300s;' '    }' '}' > /etc/nginx/conf.d/campus-task-hub.conf

nginx -t && systemctl reload nginx
```

**处理端口冲突**（实测踩坑）：若 `nginx -t` 报 `conflicting server name "_" on 0.0.0.0:8081`，说明有废弃配置占了 8081，查并移走：
```bash
grep -rl "listen 8081" /etc/nginx/        # 找冲突文件
mv /etc/nginx/conf.d/campus-test.conf /etc/nginx/conf.d/campus-test.conf.bak   # 备份移走
systemctl reload nginx
```

---

## 10. 验证

浏览器打开 **http://182.92.133.163:8081** -> 登录页。

- 方案 A：用 `admin` / `1` 登录。
- 方案 B：用 `admin` / `123456` 登录，浏览大厅（14 个任务，7 个带图）、发帖、聊天（WebSocket 实时）、管理员仲裁。
- 方案 C：用 `admin.sql` 设的账号登录。

> 本机若开了代理 / VPN，访问纯 IP 可能被拦截：把该 IP 设为直连，或临时关代理。

---

## 附 1：端口 / 防火墙

主机 iptables 默认只放行 22/80/443，**其它端口 REJECT**。8081 临时放行规则**服务器重启后会消失**：
```bash
iptables -I INPUT 7 -p tcp --dport 8081 -j ACCEPT
```
安全组（阿里云控制台）8081 已放行，不用再动。

## 附 2：域名 `campustaskhub.cn` 备案通过后

前端已用相对路径，**不用改代码**：
1. 云解析加 A 记录 -> `182.92.133.163`；
2. 第 9 步 `listen 8081;` 改 `listen 80;`，加 `server_name campustaskhub.cn www.campustaskhub.cn;`；`nginx -t && systemctl reload nginx`；
3. HTTPS：`dnf install -y certbot python3-certbot-nginx && certbot --nginx -d campustaskhub.cn -d www.campustaskhub.cn`。

## 附 3：常见问题速查

| 现象 | 排查 |
|---|---|
| `systemctl start mariadb` 失败 | **别用 MariaDB**（和宝塔冲突），用宝塔 5.7：`/etc/init.d/mysqld start` |
| 宝塔 MySQL root 密码忘了 | 跳过授权表重置，见第 7 步方案 B-2 |
| 页面能开但登录提示"用户不存在" | 忘了导 test-data.sql：`mysql -uroot -p < /root/campus/test-data.sql` |
| 后端起不来，日志报 validate 失败 | schema.sql 没导或版本旧：重导 schema.sql；确认含 `reports` 表 |
| 后端起不来，日志报 Access denied | 库密码不对：改 `/etc/campus-backend.env` 的 `SPRING_DATASOURCE_PASSWORD`，`systemctl restart campus-backend` |
| `nginx -t` 报 conflicting server name | 有废弃配置占 8081，见第 9 步冲突处理 |
| 8081 打不开、TCP 连不上 | iptables 8081 放行掉了（重跑附 1）；或本机代理拦截 |
| 聊天不实时 | WebSocket：确认 Nginx 有 `/ws/` 段（含 Upgrade 头）；F12 Network -> WS 看连接 |
| 服务器重启后 8081 不通 | iptables 规则丢了，重跑附 1；MySQL 若没自启：`/etc/init.d/mysqld start` |

## 附 4：数据策略对照

| 场景 | profile | 数据库 | 种子数据来源 | 账号 |
|---|---|---|---|---|
| 本地开发 | 默认（无 prod） | H2 内存 | `DataInitializer` 自动 | 8 用户，密码 `1` |
| 方案 A 快速验证 | 默认（无 prod） | H2 内存 | `DataInitializer` 自动 | 同上，重启清空 |
| **方案 B 课设验收** | prod | **宝塔 MySQL 5.7** | 导 `test-data.sql` | 4 账号，密码 `123456` |
| 方案 C 真实上线 | prod | 宝塔 MySQL 5.7 | 仅 `seed/admin.sql` 建管理员 | admin 自定，其余注册 |

## 附 5：安全提醒

- root 密码、数据库密码只私发、别外泄、**别提交到仓库**（本仓库是**公开仓库**，明文密码进 commit = 公开）。
- `application-prod.yml` 已在 `.gitignore`；部署密码存 `/etc/campus-backend.env`（权限 600），不进仓库。
- 服务器有 SSH 爆破（实测 1949 次失败登录），建议尽快加 SSH 公钥 + 禁用密码登录。
- 相关文档：《部署操作记录.md》（含密码，仅本地参考，勿提交）、《测试账号.md》。
