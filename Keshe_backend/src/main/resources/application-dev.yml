# 开发环境额外配置（H2 已设为默认，此文件仅覆盖开发专用选项）
spring:
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

logging:
  level:
    com.example.keshe_backend: debug

# 🔒 补上缺少的 JWT 鉴权密钥配置（从 example 模板中提取）
jwt:
  secret: "YourJwtSecretKeyAtLeast32CharactersLong"
  expiration: 86400000