# 校园任务互助平台 (Campus Task Hub) 完整功能清单与详细测试用例说明书

> 本版本已依据**实际前后端代码实现**重新核对并补全等价类划分。
> - 图例：**✅ 已实现**＝代码中确有该校验；**⚠️ 未实现/缺陷**＝需求预期该规则但代码未强制（可作为发现的缺陷或待实现项）。
> - 提示文案尽量引用代码中的真实文案（前端 `js/main.js`、`js/api.js`；后端 DTO 注解与 Service 抛出的 `BusinessException`/`ErrorCode`）。

---

## 一、 系统全功能模块清单一览（已按实现修正）

### 1. 用户认证与基本管理模块 (Auth & User Management)
*   **用户注册**：输入用户名、手机号（`^1\d{10}$`）、密码（≥6位）、确认密码，邮箱可选；用户名/手机号/邮箱唯一。
*   **用户登录**：使用**账号（用户名 / 手机号 / 邮箱任一）+ 密码**登录，校验通过后发放 JWT 令牌；登录框不区分账号类型，也不做格式校验，仅判空与账密匹配。
*   **校园实名认证**：用户提交真实姓名、学号（≤30字符）、学院，班级可选。**认证为自助即时通过**（提交即 `authStatus=1`），系统内**无管理员审核环节**，也**不要求上传学生证照片**。
*   **个人资料修改**：修改绑定手机号（格式+唯一）、绑定邮箱（格式+唯一）、头像、个人简介（超500字自动截断）、展示照片（最多5张，超出丢弃）。
*   **账户钱包与账单**：查看余额/冻结余额，模拟充值（0.01～100000元、最多2位小数），提供充值/支出/收入分页流水。

### 2. 任务发布与大厅模块 (Post & Task Hall)
*   **发布帖子**：选择发布身份 `publisherSide`（payer 出钱悬赏 / earner 收钱接活 / none 纯互助），输入标题、分类、描述、报酬描述及金额，可选截止时间（仅 payer 校验不得早于当前）、联系方式、图片。发布需已实名，管理员不可发布，30秒内同标题+描述视为重复提交；payer 发布时冻结报酬（余额不足回滚）。
*   **任务大厅检索**：按发布身份、分类（可多选）、关键词（近义词展开）模糊搜索，按时间/报酬/信用升降序排序，分页加载；已过期的 payer 帖子不展示。
*   **任务详情查看**：展示完整内容、报酬、截止时间、发布者信用评级及沟通/接单入口。

### 3. 订单管理与流转模块 (Order Management)
*   **接单/生成订单**：已实名用户在他人帖子上接单，生成订单并冻结相应资金。
*   **订单状态流转**：`pending`（待接受）→ `in_progress`（进行中）→ `completed`（双方确认完成）；`pending → cancelled`（取消）；`in_progress → disputed`（申诉）→ `closed`（管理员裁决结案）。
*   **订单申诉**：进行中订单，任一当事人填写申诉理由发起申诉，状态转为 `disputed`。

### 4. 即时通讯与聊天下单支付模块 (Chat & Payment)
*   **会话创建与管理**：确保/创建双人唯一会话，消息中心按时间倒序，支持未读红点。
*   **文本消息发送**：内容非空即可发送（当前**无字数上限**），需已实名且为会话参与者；自己发出的消息2分钟内可撤回。
*   **聊天内嵌支付**：在聊天窗发起收款(request)/转账(transfer)，金额 0.01～100000 元，不能给自己付款；若会话存在进行中订单则转账托管冻结，否则即时到账；收款卡片仅付款方可支付且不可重复支付。

### 5. 评价与举报模块 (Review & Report)
*   **订单互评**：订单 `completed` 后当事人双向评价，星级 1～5，评语可选（当前**无字数上限**），计入信用分（贝叶斯平滑）。
*   **违规内容举报**：对帖子填写举报理由（**单一文本框，无分类选项**）提交；不能举报自己的帖子，同一帖子不可重复举报。

### 6. 后台管理模块 (Admin System)
*   **申诉仲裁**：管理员对 `disputed` 订单裁决 `refund`（全退付款方）/ `settle`（全结算收款方）/ `partial`（部分结算），须填处理说明。
*   **举报/帖子处理**：查看举报聚合列表，对帖子执行下架(`close`)或删除(`delete`，软删)，原因可选并随系统通知告知发布者。
*   **说明**：系统**不含"学生认证审核"后台功能**（认证为用户自助即时通过）。

---

## 二、 全模块等价类划分测试用例详解

### 1. 用户认证与基本管理模块

#### 1.1 登录模块
> 登录仅有 `account`（账号）与 `password` 两个字段，账号为用户名/手机号/邮箱通用，**不做格式校验**。
##### 【账号输入框】
*   **有效等价类**：
    *   TC_LOG_001：输入已注册的用户名/手机号/邮箱之一（如 `13812345678`） -> 与密码匹配后登录成功，发放 JWT ✅
*   **无效等价类**：
    *   TC_LOG_002（账号为空）：账号留空点击登录 -> 前端提示"请输入用户名/手机号/邮箱和密码。" ✅
    *   TC_LOG_003（账号不存在）：输入未注册的账号 -> 提示"用户名/手机号/邮箱或密码错误"（LOGIN_FAILED 2001） ✅
##### 【密码输入框】
*   **有效等价类**：
    *   TC_LOG_004：输入与账号匹配的正确密码 -> 登录成功 ✅
*   **无效等价类**：
    *   TC_LOG_005（密码为空）：密码留空点击登录 -> 前端提示"请输入用户名/手机号/邮箱和密码。" ✅
    *   TC_LOG_006（密码错误）：账号存在但密码不符 -> 提示"用户名/手机号/邮箱或密码错误"（LOGIN_FAILED 2001，不暴露是账号还是密码错） ✅
> 说明：登录框**不做**"11位/号段/字母/特殊符号/密码长度上限"校验，原 TC_LOG_002~007、TC_LOG_010 相关用例在登录处不适用，格式类校验见注册与资料修改。

#### 1.2 注册模块
##### 【用户名输入框】
*   **有效等价类**：
    *   TC_REG_001：输入未被占用的用户名 -> 通过 ✅
*   **无效等价类**：
    *   TC_REG_002（空）：用户名留空 -> 前端"请完整填写必填信息" / 后端 @NotBlank "用户名不能为空" ✅
    *   TC_REG_003（重复）：输入已注册用户名 -> "该用户名已注册"（USERNAME_EXISTS 2002） ✅
##### 【手机号输入框】
*   **有效等价类**：
    *   TC_REG_004：输入首位为1的11位数字（如 `13812345678`） -> 通过（正则 `^1\d{10}$`） ✅
*   **无效等价类**：
    *   TC_REG_005（空）：手机号留空 -> 后端 @NotBlank "手机号不能为空" ✅
    *   TC_REG_006（含字母）：`1381234567a` -> 前端"请输入正确的 11 位手机号。" / 后端"手机号格式不正确" ✅
    *   TC_REG_007（长度过短）：`138123456`（<11位） -> 同上格式不正确 ✅
    *   TC_REG_008（长度过长）：`138123456789`（>11位） -> 同上格式不正确 ✅
    *   TC_REG_009（首位非1）：`23812345678` -> 同上格式不正确 ✅
    *   TC_REG_010（含特殊符号）：`1381234#678` -> 同上格式不正确 ✅
    *   TC_REG_011（重复）：输入已注册手机号 -> "该手机号已注册"（PHONE_EXISTS 2003） ✅
    *   （注）代码**不做**运营商号段合法性校验，故"非法号段"不作为独立无效类。
##### 【邮箱输入框（可选）】
*   **有效等价类**：
    *   TC_REG_012：不填邮箱 -> 允许通过（邮箱为可选，空存为 NULL） ✅
    *   TC_REG_013：填写合法邮箱 `student@tongji.edu.cn` -> 通过（正则 `^[^\s@]+@[^\s@]+\.[^\s@]+$`） ✅
*   **无效等价类**：
    *   TC_REG_014（缺@）：`student.tongji.edu.cn` -> 前端"请输入正确的邮箱地址。" ✅
    *   TC_REG_015（缺域名后缀点）：`student@tongji` -> 同上格式错误 ✅
    *   TC_REG_016（重复）：填写已注册邮箱 -> "该邮箱已注册"（EMAIL_EXISTS 2004） ✅
##### 【密码与确认密码框】
*   **有效等价类**：
    *   TC_REG_017：密码与确认密码均为 `pass123`（≥6位且一致） -> 通过 ✅
*   **无效等价类**：
    *   TC_REG_018（密码过短）：`12345`（<6位） -> "密码长度不能少于 6 位。" ✅
    *   TC_REG_019（密码为空）：密码留空 -> 后端 @NotBlank "密码不能为空" ✅
    *   TC_REG_020（两次不一致）：密码 `pass123`、确认 `pass456` -> "两次输入的密码不一致。" ✅
    *   TC_REG_021（确认密码为空）：确认框留空 -> 前端必填校验拦截 ✅
    *   ⚠️ TC_REG_022（密码超长20位以上）：代码**无上限**，超长密码可通过。需求若要求上限，则此为**待实现**。
##### 【昵称/敏感词】
*   ⚠️ TC_REG_023（昵称长度≤10 / 敏感词过滤）：代码中**无独立昵称字段，也无长度与敏感词校验**。若需求要求，则属**待实现/缺陷**。

#### 1.3 校园实名认证
> 表单字段：真实姓名、学号、学院（必填），班级（可选）。**无学生证照片上传，无管理员审核，提交即通过。**
##### 【学号输入框】
*   **有效等价类**：
    *   TC_AUTH_001：输入长度 ≤30 的学号（如 `2023110245`） -> 通过，`authStatus` 置为已认证 ✅
*   **无效等价类**：
    *   TC_AUTH_002（空）：任一必填项（姓名/学号/学院）留空 -> 前端"请完整填写认证信息。" ✅
    *   TC_AUTH_003（学号超长）：学号长度 >30 字符 -> "学号格式不合法"（后端拦截） ✅
##### 【姓名 / 学院】
*   **有效**：TC_AUTH_004：姓名、学院均非空 -> 通过 ✅
*   **无效**：TC_AUTH_005：姓名或学院为空 -> "请完整填写认证信息。" ✅
> 说明：原"学生证照片格式/大小、管理员审核通过/驳回"相关用例（旧 TC_AUTH_004~006、TC_ADM_001~003）**在当前实现中不存在**，已移除；若功能需求要求，则列为**待实现**。通用图片上传规则见 1.6。

#### 1.4 个人资料修改
##### 【新手机号】
*   **有效**：TC_PROF_001：`13800000000` -> 通过 ✅
*   **无效**：
    *   TC_PROF_002（格式错）：不满足 `^1\d{10}$` -> 前端"请输入正确的 11 位手机号。" / 后端"手机号格式不正确" ✅
    *   TC_PROF_003（空）：留空 -> @NotBlank "手机号不能为空" ✅
    *   TC_PROF_004（重复）：已被他人使用 -> "该手机号已被其他账号使用"（PHONE_EXISTS 2003） ✅
##### 【新邮箱】
*   **有效**：TC_PROF_005：`student@tongji.edu.cn` -> 通过 ✅
*   **无效**：
    *   TC_PROF_006（缺@）：`student.tongji.edu.cn` -> "邮箱格式不正确" ✅
    *   TC_PROF_007（缺后缀）：`student@tongji` -> "邮箱格式不正确" ✅
    *   TC_PROF_008（空）：留空 -> @NotBlank "邮箱不能为空" ✅
    *   TC_PROF_009（重复）：已被他人使用 -> "该邮箱已被其他账号使用"（EMAIL_EXISTS 2004） ✅
##### 【个人简介 / 展示照片（边界行为）】
*   TC_PROF_010（简介超长）：bio 超过500字符 -> **不报错，自动截断至500字符** ✅
*   TC_PROF_011（展示照片超量）：上传超过5张 -> **不报错，仅保留前5张** ✅

#### 1.5 账户钱包充值
##### 【充值金额框】
*   **有效等价类**：
    *   TC_WAL_001：输入 `0.01 ～ 100000` 的正数（如 `100`） -> 成功拉起模拟支付 ✅
    *   TC_WAL_002：带1~2位小数（如 `50.50`） -> 通过 ✅
    *   TC_WAL_003（下边界）：`0.01` -> 通过；`100000` -> 通过（含边界值） ✅
*   **无效等价类**：
    *   TC_WAL_004（零或负数）：`0` 或 `-20` -> "充值金额需大于 0" / 前端"请输入正确的充值金额。"（金额需 ≥0.01） ✅
    *   TC_WAL_005（非数字）：`100元`、`50abc` -> 前端"请输入正确的充值金额。" ✅
    *   TC_WAL_006（超上限）：`100000.01` 或 `99999999` -> "单笔充值不能超过 100,000"（DecimalMax，**上限为100000元，非10000**） ✅
    *   TC_WAL_007（小数位过多）：`10.123`（>2位小数） -> "金额最多支持两位小数"（RECHARGE_AMOUNT_INVALID 5001） ✅

#### 1.6 通用图片上传（头像/展示照片/聊天图片/帖子图片）
> 后端 `/api/upload` 统一校验，非"仅JPG/PNG、5MB"。
*   **有效等价类**：
    *   TC_IMG_001：上传任意 `image/*`（jpg/png/gif/webp）且 ≤10MB -> 成功返回图片 URL ✅
*   **无效等价类**：
    *   TC_IMG_002（非图片）：上传 `.exe`/`.txt` 等非 image 类型 -> "只能上传图片文件" ✅
    *   TC_IMG_003（超大）：单张 >10MB -> "图片不能超过 10MB" ✅
    *   TC_IMG_004（空文件）：未选择文件 -> "上传文件为空" ✅

---

### 2. 任务发布与大厅模块

#### 2.1 发布帖子
##### 【发布身份 publisherSide】
*   **有效**：TC_TASK_001：选择 `payer`/`earner`/`none` 之一 -> 通过 ✅
*   **无效**：TC_TASK_002：为空或非以上三者 -> "发布身份不能为空" / "publisherSide 必须为 payer/earner/none" ✅
##### 【任务标题】
*   **有效**：TC_TASK_003：填写非空标题（如"东区食堂代购螺蛳粉"） -> 通过 ✅
*   **无效**：TC_TASK_004（空）：标题留空 -> 前端"请填写标题。" / 后端"标题不能为空" ✅
*   ⚠️ TC_TASK_005（长度5~30）：代码仅 `@NotBlank`，**未限制标题长度**（DB上限200）。需求若要求5~30字，则属**待实现/缺陷**。
##### 【任务分类】
*   **有效**：TC_TASK_006：选择一个分类 -> 通过 ✅
*   **无效**：TC_TASK_007（未选）：不选分类 -> 前端"请选择分类。" / 后端"分类不能为空" ✅
##### 【任务描述】
*   **有效**：TC_TASK_008：填写非空描述 -> 通过 ✅
*   **无效**：TC_TASK_009（空）：描述留空 -> 前端"请填写描述。" / 后端"描述不能为空" ✅
*   ⚠️ TC_TASK_010（长度10~500）：代码仅 `@NotBlank`，**未限制描述长度**。需求若要求，则属**待实现/缺陷**。
##### 【报酬金额（payer 悬赏冻结）】
*   **有效**：TC_TASK_011：payer 帖子填写小于等于可用余额的报酬 -> 发布成功并冻结相应金额 ✅
*   **无效**：
    *   TC_TASK_012（报酬描述空）：`reward` 留空 -> "报酬描述不能为空" ✅
    *   TC_TASK_013（余额不足）：payer 帖报酬 > 可用余额 -> "余额不足"（BALANCE_NOT_ENOUGH 3001），整体回滚不发布 ✅
##### 【截止时间（仅 payer 校验）】
*   **有效**：TC_TASK_014：payer 帖截止时间晚于当前时间 -> 通过 ✅
*   **无效**：TC_TASK_015（早于当前）：payer 帖截止时间早于当前时间 -> "该悬赏已过截止时间"（POST_EXPIRED 3006） ✅
##### 【防重复提交】
*   TC_TASK_016：30秒内提交完全相同的标题+描述 -> 被判为重复提交拦截（PARAM_ERROR） ✅
##### 【图片】
*   TC_TASK_017：最多上传3张图片（前端限制），单张遵循 1.6 通用上传规则 ✅

#### 2.2 任务大厅检索与筛选
##### 【搜索/分类/排序】
*   **有效等价类**：
    *   TC_HALL_001：输入存在的关键词（如"快递"，含近义词展开） -> 展示匹配任务 ✅
    *   TC_HALL_002：勾选一个或多个分类 -> 仅展示对应分类任务 ✅
    *   TC_HALL_003：切换发布身份筛选（全部/我要花钱/我能赚钱/纯互助）与排序（时间/报酬/信用 升降） -> 列表按条件刷新 ✅
*   **无效/边界等价类**：
    *   TC_HALL_004（无匹配）：输入无意义乱码（如"asdfghjkl12345"） -> "暂无符合条件的任务，请尝试调整筛选条件。" ✅
    *   TC_HALL_005（过期帖不展示）：已过截止时间的 payer 帖 -> 不出现在大厅列表 ✅

---

### 3. 订单管理与流转模块

#### 3.1 接单 / 生成订单
##### 【接单资格与动作控制】
*   **有效等价类**：
    *   TC_ORD_001：已实名的非发布者、非管理员用户，在 `open` 且未过期的帖子上接单 -> 接单成功，相应资金冻结 ✅
*   **无效等价类**：
    *   TC_ORD_002（自我接单）：在自己发布的帖子上接单 -> "不能对自己发布的帖子下单/接单"（SELF_DEALING 3005） ✅
    *   TC_ORD_003（未实名）：未实名用户接单 -> "请先完成实名认证后再操作"（VERIFICATION_REQUIRED 2006） ✅
    *   TC_ORD_004（管理员接单）：管理员账号接单 -> "管理员账号不参与交易"（FORBIDDEN） ✅
    *   TC_ORD_005（帖子不存在/已取消）：对已删除/关闭的帖子接单 -> "任务不存在或已取消"（TASK_NOT_FOUND_OR_CANCELLED 3003） ✅
    *   TC_ORD_006（已过期）：对已过截止时间的 payer 帖接单 -> "该悬赏已过截止时间，无法接单"（POST_EXPIRED 3006） ✅
    *   TC_ORD_007（重复接单/已被抢占）：payer 帖已存在进行中订单时再接单 -> "该帖子已有进行中的订单"（DUPLICATE_ORDER 3008） ✅
    *   TC_ORD_008（缺参数）：`postId` 或 `chatId` 为空 -> "帖子ID不能为空" / "会话ID不能为空" ✅

#### 3.2 订单状态流转操作（权限 + 状态双校验）
##### 【接受订单 acceptOrder】
*   **有效**：TC_ORD_009：帖子发布者对 `pending` 订单点击接受，且付款方余额充足 -> 订单转 `in_progress` ✅
*   **无效**：
    *   TC_ORD_010（非发布者）：非发布者点击接受 -> "无权访问该资源"（FORBIDDEN） ✅
    *   TC_ORD_011（状态非pending）：对非 `pending` 订单接受 -> "订单当前状态不可操作"（INVALID_ORDER_STATUS 3007） ✅
    *   TC_ORD_012（余额不足）：付款方余额不足 -> "余额不足"（BALANCE_NOT_ENOUGH 3001） ✅
##### 【取消订单 cancelOrder】
*   **有效**：TC_ORD_013：订单当事人对 `pending` 订单取消 -> 转 `cancelled`，冻结资金释放 ✅
*   **无效**：
    *   TC_ORD_014（状态非pending）：对 `in_progress` 等订单取消 -> INVALID_ORDER_STATUS 3007 ✅
    *   TC_ORD_015（非当事人）：非参与者取消 -> "你不是该订单的参与者"（FORBIDDEN） ✅
##### 【确认完成 confirmOrder】
*   **有效**：TC_ORD_016：当事人对 `in_progress` 订单确认完成 -> 双方确认后转 `completed`，资金结算给收款方 ✅
*   **无效**：
    *   TC_ORD_017（状态非in_progress）：对非进行中订单确认 -> INVALID_ORDER_STATUS 3007 ✅
    *   TC_ORD_018（非当事人）：非参与者确认 -> FORBIDDEN ✅

#### 3.3 订单申诉
##### 【申诉原因】
*   **有效**：TC_DIS_001：`in_progress` 订单当事人填写非空申诉理由 -> 提交成功，订单转 `disputed` ✅
*   **无效**：
    *   TC_DIS_002（理由为空）：不填理由提交 -> "请填写申诉理由"（@NotBlank / PARAM_ERROR） ✅
    *   TC_DIS_003（状态不符）：对非 `in_progress` 订单申诉 -> INVALID_ORDER_STATUS 3007 ✅
    *   TC_DIS_004（非当事人越权）：拼装他人订单ID申诉 -> "你不是该订单的参与者"（FORBIDDEN） ✅

---

### 4. 即时通讯与聊天下单支付模块

#### 4.1 文本消息发送
##### 【聊天文本输入框】
*   **有效等价类**：
    *   TC_CHAT_001：输入非空文本发送 -> 实时渲染，对方收到并弹未读红点 ✅
*   **无效等价类**：
    *   TC_CHAT_002（空消息）：内容为空/纯空白 -> 前端不触发发送；后端 @NotBlank "消息内容不能为空" ✅
    *   TC_CHAT_003（未实名）：未实名用户发消息 -> "请先完成实名认证后再操作"（VERIFICATION_REQUIRED） ✅
    *   TC_CHAT_004（非会话参与者）：非参与者向会话发消息 -> "不是会话参与者"（FORBIDDEN） ✅
    *   ⚠️ TC_CHAT_005（超长消息）：代码**未限制单条消息字数**（无2000/500上限）。需求若要求，则属**待实现/缺陷**。
##### 【消息撤回】
*   **有效**：TC_CHAT_006：撤回自己2分钟内发出的消息 -> 撤回成功 ✅
*   **无效**：TC_CHAT_007（超时撤回）：撤回超过2分钟的消息 -> "已超过2分钟，无法撤回"（WITHDRAW_TIMEOUT 4001） ✅

#### 4.2 聊天内嵌支付/转账
##### 【收款/转账金额框】
*   **有效等价类**：
    *   TC_PAY_001：发起 `transfer`/`request`，金额 0.01～100000 且（转账时）余额充足 -> 成功；有进行中订单则托管冻结，否则即时到账 ✅
*   **无效等价类**：
    *   TC_PAY_002（超额转账）：转账金额 > 可用余额 -> "余额不足"（BALANCE_NOT_ENOUGH 3001） ✅
    *   TC_PAY_003（金额≤0）：金额 `0` 或负数 -> "金额需大于 0"（DecimalMin 0.01） ✅
    *   TC_PAY_004（金额超上限）：金额 >100000 -> "金额不能超过 100,000"（DecimalMax） ✅
    *   TC_PAY_005（给自己付款）：`partnerId` = 当前用户 -> "不能给自己付款"（PARAM_ERROR） ✅
    *   TC_PAY_006（kind非法）：`kind` 非 `request`/`transfer` -> "kind 必须为 request 或 transfer" ✅
    *   TC_PAY_007（管理员转账）：管理员发起转账 -> "管理员不参与交易"（FORBIDDEN） ✅
##### 【收款卡片支付 payPaymentCard】
*   **有效**：TC_PAY_008：付款方对 `pending` 收款卡片支付且余额充足 -> 扣款成功 ✅
*   **无效**：
    *   TC_PAY_009（重复支付）：对已处理卡片再次支付 -> "该收款已处理"（PAYMENT_ALREADY_PROCESSED 4002） ✅
    *   TC_PAY_010（非付款方）：非付款方点击支付 -> "只有付款方才能支付"（FORBIDDEN） ✅
    *   TC_PAY_011（余额不足）：付款方余额不足 -> BALANCE_NOT_ENOUGH 3001 ✅

---

### 5. 评价与举报模块

#### 5.1 订单评价
##### 【星级评分】
*   **有效**：TC_REV_001：选择 1~5 星并提交（评语可留空） -> 保存成功，重算被评价者信用分 ✅
*   **无效**：
    *   TC_REV_002（星级<1）：评分为 0 -> "评分至少为 1"（@Min 1）✅
    *   TC_REV_003（星级>5）：评分为 6 -> "评分最多为 5"（@Max 5）✅
    *   （注）前端默认选中5星，需接口层构造 0/6 星验证边界。
##### 【评价前置条件】
*   **无效等价类**：
    *   TC_REV_004（订单未完成）：对非 `completed` 订单评价 -> "订单未完成，无法评价"（INVALID_ORDER_STATUS） ✅
    *   TC_REV_005（重复评价）：对同一订单二次评价 -> "已评价过该订单"（CONFLICT） ✅
    *   TC_REV_006（非当事人）：非订单参与者评价 -> "不是订单参与者，无法评价"（FORBIDDEN） ✅
    *   TC_REV_007（被评价者非对方）：`toUserId` 非订单另一方 -> "被评价者不是订单参与者"（FORBIDDEN） ✅
##### 【评语内容】
*   ⚠️ TC_REV_008（评语长度≤200）：`content` 为可选字段且**无长度校验**。需求若要求 ≤200字，则属**待实现/缺陷**。

#### 5.2 违规内容举报
> 举报只有一个 `reason` 文本框，**无"举报分类/类型"单选**。
##### 【举报理由】
*   **有效**：TC_REP_001：登录的非管理员，对他人帖子填写非空理由提交 -> 举报成功，进入 pending 待处理 ✅
*   **无效**：
    *   TC_REP_002（理由为空）：理由留空提交 -> "请填写举报理由"（@NotBlank / PARAM_ERROR） ✅
    *   TC_REP_003（未登录）：未登录举报 -> 前端"请先登录后再举报。" / 后端 AUTH_REQUIRED ✅
    *   TC_REP_004（举报自己）：举报自己发布的帖子 -> "不能举报自己发布的帖子"（PARAM_ERROR） ✅
    *   TC_REP_005（重复举报）：对同一帖子再次举报（已有 pending） -> "你已举报过该帖子，管理员会尽快处理"（CONFLICT） ✅
    *   TC_REP_006（管理员举报）：管理员发起举报 -> "管理员账号无需举报"（FORBIDDEN） ✅
    *   TC_REP_007（帖子不存在）：举报已删除帖子 -> "帖子不存在"（TASK_NOT_FOUND_OR_CANCELLED） ✅
*   ⚠️ TC_REP_008（举报分类必选）：系统**无举报分类字段**，原"未选择举报类型"用例不适用；若需求要求分类，则属**待实现**。

---

### 6. 后台管理模块

#### 6.1 申诉仲裁处理
##### 【裁决方式与资金流向】
*   **有效等价类**：
    *   TC_ADM_001（全额退款）：对 `disputed` 订单选 `refund` 并填处理说明 -> 冻结金额全额退付款方，订单转 `closed` ✅
    *   TC_ADM_002（全额结算）：选 `settle` 并填说明 -> 冻结金额全额结算给收款方，订单 `closed` ✅
    *   TC_ADM_003（部分结算）：选 `partial`，金额满足 `0 < 金额 < 订单总额` -> 指定金额给收款方、余额退付款方 ✅
*   **无效等价类**：
    *   TC_ADM_004（处理说明空）：`note` 留空 -> "请填写处理说明"（@NotBlank） ✅
    *   TC_ADM_005（decision非法）：非 `refund`/`settle`/`partial` -> "无效的处理方式"（PARAM_ERROR） ✅
    *   TC_ADM_006（部分结算金额越界）：`partial` 金额 ≤0 或 ≥订单总额 -> "部分结算金额需大于 0 且小于订单金额 X 元" ✅
    *   TC_ADM_007（订单非争议态/并发处理）：对非 `disputed` 订单裁决，或两名管理员并发、后者提交时订单已被处理 -> "该订单不在争议处理中"（INVALID_ORDER_STATUS，靠状态判定实现并发防护，非乐观锁版本号） ✅
    *   TC_ADM_008（越权）：非管理员访问 `/api/admin/**` -> 被 SecurityConfig 拦截（FORBIDDEN/UNAUTHORIZED） ✅

#### 6.2 举报/帖子处理
##### 【下架 / 删除帖子】
*   **有效等价类**：
    *   TC_ADM_009（下架）：对被举报帖子执行 `close` -> 帖子转 `closed`，大厅不再展示，清该帖 pending 举报，系统通知发布者 ✅
    *   TC_ADM_010（删除）：执行 `delete` -> 软删（设 `deletedAt`），全站不可见 ✅
    *   TC_ADM_011（带原因）：填写下架/删除原因 -> 原因随系统通知发送给发布者 ✅
*   **边界等价类**：
    *   TC_ADM_012（原因可空）：不填原因直接下架/删除 -> **允许通过**（`reason` 为可选，`@RequestBody(required=false)`），不阻断操作 ✅
    *   TC_ADM_013（帖子不存在）：处理已删除帖子 -> "帖子不存在" ✅

> 说明：原"学生认证审核通过/驳回/驳回理由必填"（旧 TC_ADM_001~003）在当前系统中**无对应后台功能**（认证为自助即时通过），已整体移除；如需该功能，请作为**待实现需求**单列。

---

## 三、 测试文档与实际实现差异汇总（重点核对结论）

### A. 描述了系统中不存在的功能/规则（原用例不可执行，已修正/移除）
| 原用例 | 问题 | 实际情况 |
|--------|------|----------|
| TC_LOG_002~007 | 登录框做手机号格式/号段/长度校验 | 登录用通用 `account` 字段，仅判空+账密匹配，无格式校验 |
| TC_LOG_010 | 密码长度上限20位 | 仅下限 ≥6，无上限 |
| TC_REG_005/006 | 昵称≤10、敏感词过滤 | 无独立昵称字段，无长度/敏感词校验 |
| TC_AUTH_004~006 | 学生证照片 仅JPG/PNG ≤5MB | 认证不需上传照片；通用图片上传实为 image/* 且 ≤10MB |
| TC_ADM_001~003 | 管理员审核学生认证 | 认证为自助即时通过，无审核后台 |
| TC_REP_002 | 举报需选分类 | 举报仅单一 `reason` 文本框，无分类 |
| TC_ADM_006 | 并发用乐观锁提示 | 靠订单状态判定，提示"该订单不在争议处理中" |

### B. 边界值/文案错误（已订正）
| 项 | 原文档 | 实际代码 |
|----|--------|----------|
| 单笔充值上限 | 10,000 元 | **100,000 元**（`@DecimalMax`） |
| 手机号规则 | 分"字母/特殊符号/号段"多条提示 | 统一正则 `^1\d{10}$`，单一格式提示，无号段校验 |
| 聊天转账上限 | 未提及 | 100,000 元，且下限 0.01 |

### C. ⚠️ 需求预期但代码未强制（建议作为"发现的缺陷/待实现"跟踪）
1. 发布任务：标题长度（5~30）、描述长度（10~500）**未做限制**（仅非空，DB标题上限200）。
2. 评价：评语长度（≤200）**未做限制**（content 可选）。
3. 聊天：单条消息字数上限（2000/500）**未做限制**。
4. 注册：密码长度上限、昵称长度与敏感词**未做**。
5. 举报：举报分类字段**缺失**。

### D. 本次新增补全的等价类（原文档缺失，已补入第二节）
- 注册：用户名唯一、手机号唯一、邮箱可选+格式+唯一。
- 通用图片上传：类型/大小/空文件（1.6）。
- 发布：`publisherSide` 枚举、payer 余额冻结不足、payer 截止时间早于当前、30秒防重复提交。
- 订单：全状态流转（接受/取消/确认）的状态+权限+余额多维校验、缺参数、管理员接单。
- 聊天支付：给自己付款、金额上下限、`kind` 非法、收款卡片重复支付/非付款方/余额不足、消息2分钟撤回时限。
- 评价：订单未完成、重复评价、非当事人、被评价者非对方、星级边界（0/6）。
- 举报：举报自己、重复举报、管理员举报、帖子不存在。
- 仲裁：refund/settle/partial 三向资金流、部分结算金额边界、处理说明必填、并发状态防护、越权访问。
